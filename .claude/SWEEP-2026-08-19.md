# SWEEP 2026-08-19 — the six reported items, located and verified

Source: workflow `wf_229bc5ec-167`, 9 agents, 896k tokens, 0 errors. Read-only investigation.
**Nothing here is fixed yet.** This is the evidence, banked before the quota ran out.


---

## ?

**Verdict:** Both screens are real and both live in src/StockOpnameView.jsx; the cause is the same two shapes — (1) hardcoded non-token plates (`bg-[#1a1a1a]`, `bg-black/40`, `bg-[#111]`) that never flip with the theme while the ink on them is a token that does, and (2) a token PLATE set on a parent (`bg-[var(--gold)]`) with a token INK set on a child (`text-[var(--accent-ink)]` / `text-[var(--ink-dim)]`), which in dark mode is the identical hex #D08A2E on #D08A2E (1.00:1).

### Mechanism

SCREEN A is the "monitor" view-mode product grid in StockOpnameView.jsx (viewMode === 'monitor', header "Supply Telemetry", facility select defaults to "Master Vault (HQ)"), lines 664-747. The card plate is hardcoded: `bg-[#1a1a1a] border border-[#2a2a2a]` (L671); the header band is `bg-black/40` (L682); the VAULT/INITIAL row is `bg-black/60` (L693); the three breakdown tiles are `bg-[#111] border border-[#2a2a2a]` (L714, L719, L724); the progress rail is `bg-[#111]` (L731). None of those are tokens, so they stay near-black in BOTH themes. The ink on them IS tokenised and does flip: product title `text-[var(--ink)]` (L684) = #E8E4DE dark / #131211 light; ID line `text-[var(--ink-dim)]` (L685) = #928B81 dark / #3F3B36 light; the VAULT / INITIAL label `text-[11px] text-[var(--ink-dim)]` (L695); the big number `text-2xl font-black text-[var(--ink)] font-mono` with `hover:text-[var(--ink-dim)]` for DEVELOPER/COMPANY_OWNER (L698); the "/ initial" figure `text-[var(--ink-dim)]` (L705); "Status" label `text-[var(--ink-dim)]` (L709). Result in light mode: near-black ink on a still-black card. In dark mode the dim labels are the weak ones — #928B81 at text-[11px] on `bg-black/60` — plus one genuine both-theme failure on the same card: the HEALTHY chip is `bg-[var(--gold)] text-[var(--ink-dim)] border border-[var(--line)]` (L713), a gold plate carrying dim ink (#D08A2E on #928B81 dark; #3F3B36 on #7A4C0C light). Two elements also carry two competing border-colour classes on one node — L682 `border-b border-[#2a2a2a] ... border-[var(--line)]`, L683 `border border-[#333] ... border-[var(--line)]`, L731 `border-t border-[#2a2a2a] border-[var(--line)]` — so which colour wins is decided by generated-CSS order, not by class order.

SCREEN B is viewMode === 'quarantine' in the same file, plus the shared tab strip above it. Tab strip (L633-646): the container is `bg-black/50 rounded-lg p-1 border border-[var(--line)]` — a hardcoded black plate that does not flip. Each of MONITOR (L634), HQ AUDITS (L637), QUARANTINE (L640), NEW COUNT (L643) is active `bg-[var(--gold)] text-[var(--gold-ink)] shadow-md`, inactive `text-[var(--ink-dim)] hover:text-[var(--ink)]`. In light mode the three inactive tabs are #3F3B36 ink on a 50%-black plate. The HQ Audits count badge is `bg-[var(--danger)] text-[var(--gold-ink)]` (L637).

Quarantine panel shell (L756-757): `bg-black/40 ... border border-[var(--accent-edge)]`, overlaid with a hardcoded orange radial `rgba(249,115,22,0.05)`. Sub-tabs (L763-768): "Active Quarantine" active `bg-[var(--gold)] text-[var(--gold-ink)]`, inactive `text-[var(--ink-dim)] hover:text-[var(--accent-ink)]`; "Liquidation History" active `bg-[var(--raised)] text-[var(--ink)]`, inactive `text-[var(--ink-dim)] hover:text-[var(--ink)]`; both sit inside `bg-[var(--sunk)] border border-[var(--accent-edge)]` (L762).

SUNK CAPITAL (DEAD ASSET VALUE) card — this is the parent-plate/child-ink shape exactly: the wrapper div is `bg-[var(--gold)] p-3 rounded-lg border border-[var(--accent-edge)]` (L789); the label is `text-[11px] text-[var(--accent-ink)] uppercase font-bold` (L790); the money figure is `text-xl font-black text-[var(--accent-ink)] font-mono` (L791). In DARK, --gold and --accent-ink are both #D08A2E — literally the same hex, 1.00:1. In LIGHT it is #4F3603 on #7A4C0C. Broken in both themes, and the plate token and ink token are on different elements so no single-element scan sees the pair.

The same shape repeats on the quarantine row icon puck: `p-3 bg-[var(--gold)] text-[var(--accent-ink)] rounded-full border border-[var(--accent-edge)]` (L809) — here plate and ink are on the SAME element and it is still #D08A2E on #D08A2E in dark.

CONVERT TO SAMPLE (L825): `bg-[var(--gold)] hover:bg-[var(--gold)] border border-[var(--line)] text-[var(--ink-dim)] hover:text-[var(--ink)]`. RTV FACTORY (L828): `bg-[var(--gold)] hover:bg-[var(--gold)] border border-[var(--line)] text-[var(--ink-dim)] hover:text-[var(--gold-ink)]`. Both are gold plate + dim ink at text-[11px] font-black: #D08A2E/#928B81 in dark, #7A4C0C/#3F3B36 in light. The neighbouring PENALTY CHARGE button (L831) is the correctly-paired one: `bg-[var(--danger-well)] hover:bg-[var(--danger)] border border-[var(--danger)] text-[var(--danger-ink)] hover:text-[var(--gold-ink)]`. The row itself is `bg-[var(--sunk)] border border-[var(--line)]` (L807) with `text-[var(--ink)]` name (L811) and `text-[var(--accent-ink)]` damaged count (L813).

The resolution modal's submit button carries the same defect (L604): SAMPLING → `bg-[var(--gold)] hover:bg-[var(--gold)] text-[var(--ink)]` (pale ink on a gold plate in dark, near-black ink on a dark-gold plate in light), RTV → `bg-[var(--gold)] ... text-[var(--gold-ink)]` (this one is correct), PENALTY → `bg-[var(--danger)] ... text-[var(--gold-ink)]`.

Why the audit missed it: src/config/contrast.selfcheck.mjs is a hand-written list of token PAIRS. It contains ['readout ON the gold plate','gold-ink','gold',4.5] and ['the ON icon on its plate','gold-ink','gold',4.5] — i.e. it only ever checks gold-ink against gold. There is no pair for accent-ink-on-gold or ink-dim-on-gold, and hardcoded plates (#1a1a1a, black/40, #111) are not tokens so they cannot appear in the list at all. theme.css L428-435 already documents this exact class of bug ("a palette sweep must cover the colour NAMES too... bg-black / text-white are not hexes"); Screen A is that bug, unswept.

THEME.CSS TOKENS THAT EXIST (dark value at :root, light value in the light block; names exactly as written):
normal ink — --ink (#E8E4DE / #131211), --ink-muted (#A39B90 / #3A3733), --ink-inverse (#0A0908 / #F4F1EB), --select-ink, --duke-ink-hi, --duke-on-fill
dim ink — --ink-dim (#928B81 / #3F3B36), --duke-ink-1 … --duke-ink-8, --duke-on-paper-dim
disabled ink — --ink-disabled (#57514A / #7E7A74) [deliberately below the AA floor, documented at theme.css L88-91]
accent ink — --accent-ink (#D08A2E / #4F3603), --accent-edge (#D08A2E / #7A5A12), --duke-amber-ink, --duke-amber-ink-2, --duke-amber-on-paper, --duke-amber-edge, --duke-amber-edge-2, --duke-brass-ink, --duke-brass-ink-2, --duke-price-ink, --shell-orange-ink, --shell-orange-edge
gold — --gold (#D08A2E / #7A4C0C), --tier-gold, --hatch-gold, --duke-brass, --duke-brass-2, --duke-brass-3, --duke-brass-edge, --duke-brass-edge-2, --duke-brass-edge-3, --duke-brass-edge-4
gold-ink — --gold-ink (#0A0908 / #FCF7EE)
amber — --amber (#F59E0B / #92400E), --duke-amber (#ff9d00), --duke-amber-2 (#c47f00)
raised — --raised (#1B1917 / #EDE7D8); neighbours --ground, --panel, --sunk, --glass-solid, --grad-panel
inset — --inset (#0E0D0C / #C6BDA9); related recess tokens --duke-fill-well, --duke-fill-deep, --duke-well, --duke-well-solid, --duke-veil, --duke-veil-2
line — --line (#26231F / #6E6A64), --line-2 (#3E3A35 / #5E5A55), --line-3 (#6A645C / #46423C), --glass-edge, --glass-edge-2, --duke-edge-1 … --duke-edge-5, --duke-edge-ctl, --duke-veil-edge, --duke-veil-edge-2, --hatch
danger family — --danger (#B4524A / #9E4038), --danger-ink (#E08C82 / #611A14), --danger-text (#E08C82 / #7E2A22), --danger-plate (#8E4038 both), --danger-plate-ink (#F7F2EE both), --danger-rail (#B4524A / #A8443A), --danger-badge (#B4524A both), --danger-well (#1E1512 / #D8C4BF), --hatch-danger, --duke-danger-ink, --duke-danger-ink-2, --duke-danger-edge, --tier-apex

### Citations

| file | line | what |
|---|---|---|
| `src/StockOpnameView.jsx` | 671 | Screen A card plate hardcoded: bg-[#1a1a1a] border border-[#2a2a2a] — never flips with theme |
| `src/StockOpnameView.jsx` | 682 | Screen A card header band bg-black/40, and two competing border-colour classes (border-[#2a2a2a] + border-[var(--line)]) on one node |
| `src/StockOpnameView.jsx` | 684 | Screen A product title: font-black text-[var(--ink)] text-sm uppercase truncate drop-shadow-md |
| `src/StockOpnameView.jsx` | 685 | Screen A ID line: text-[10px] text-[var(--ink-dim)] font-mono |
| `src/StockOpnameView.jsx` | 693 | Screen A VAULT/INITIAL row plate: bg-black/60 border border-[var(--line)] — hardcoded black |
| `src/StockOpnameView.jsx` | 695 | Screen A 'Vault / Initial' label: text-[11px] text-[var(--ink-dim)] font-bold uppercase tracking-widest |
| `src/StockOpnameView.jsx` | 698 | Screen A big stock number: text-2xl font-black text-[var(--ink)] font-mono, hover:text-[var(--ink-dim)] for DEVELOPER/COMPANY_OWNER |
| `src/StockOpnameView.jsx` | 705 | Screen A '/ initial' secondary figure: text-sm font-bold text-[var(--ink-dim)] font-mono |
| `src/StockOpnameView.jsx` | 713 | Screen A HEALTHY chip: gold plate with dim ink — bg-[var(--gold)] text-[var(--ink-dim)] border border-[var(--line)] |
| `src/StockOpnameView.jsx` | 714 | Screen A breakdown tiles hardcoded bg-[#111] border border-[#2a2a2a] carrying text-[var(--accent-ink)] (also L719 ink-dim, L724 danger-ink) |
| `src/StockOpnameView.jsx` | 633 | Screen B tab strip container: bg-black/50 rounded-lg p-1 border border-[var(--line)] — hardcoded black plate |
| `src/StockOpnameView.jsx` | 634 | MONITOR tab: active bg-[var(--gold)] text-[var(--gold-ink)]; inactive text-[var(--ink-dim)] hover:text-[var(--ink)] (same pattern L637 HQ Audits, L640 Quarantine, L643 New Count) |
| `src/StockOpnameView.jsx` | 756 | Screen B panel shell: bg-black/40 border border-[var(--accent-edge)]; L757 hardcoded orange radial rgba(249,115,22,0.05) |
| `src/StockOpnameView.jsx` | 763 | Active Quarantine sub-tab: bg-[var(--gold)] text-[var(--gold-ink)] / inactive text-[var(--ink-dim)] hover:text-[var(--accent-ink)] inside bg-[var(--sunk)] border-[var(--accent-edge)] (L762) |
| `src/StockOpnameView.jsx` | 789 | SUNK CAPITAL card PARENT plate: bg-[var(--gold)] p-3 rounded-lg border border-[var(--accent-edge)] |
| `src/StockOpnameView.jsx` | 790 | SUNK CAPITAL child ink: text-[11px] text-[var(--accent-ink)] — accent-ink === gold (#D08A2E) in dark, 1.00:1 |
| `src/StockOpnameView.jsx` | 791 | SUNK CAPITAL money figure: text-xl font-black text-[var(--accent-ink)] font-mono on the same gold parent |
| `src/StockOpnameView.jsx` | 809 | Quarantine row icon puck: bg-[var(--gold)] text-[var(--accent-ink)] border border-[var(--accent-edge)] — same hex on same hex in dark |
| `src/StockOpnameView.jsx` | 825 | CONVERT TO SAMPLE: bg-[var(--gold)] hover:bg-[var(--gold)] border border-[var(--line)] text-[var(--ink-dim)] hover:text-[var(--ink)] |
| `src/StockOpnameView.jsx` | 828 | RTV FACTORY: bg-[var(--gold)] hover:bg-[var(--gold)] border border-[var(--line)] text-[var(--ink-dim)] hover:text-[var(--gold-ink)] |
| `src/StockOpnameView.jsx` | 831 | PENALTY CHARGE, the correctly-paired neighbour: bg-[var(--danger-well)] text-[var(--danger-ink)] border border-[var(--danger)] |
| `src/StockOpnameView.jsx` | 604 | Resolution modal submit: SAMPLING branch is bg-[var(--gold)] text-[var(--ink)] — same gold-plate/wrong-ink defect |
| `src/styles/theme.css` | 95 | --gold #D08A2E and --gold-ink #0A0908 (dark), with the comment 'gold — decoration only ... Not for text' |
| `src/styles/theme.css` | 110 | --accent-ink #D08A2E in dark — identical hex to --gold, which is why accent-ink on a gold plate is 1.00:1 |
| `src/styles/theme.css` | 244 | light-mode --gold #7A4C0C / --gold-ink #FCF7EE — the plate inverts in light, so ink placed on it must invert too |
| `src/styles/theme.css` | 262 | light-mode --accent-ink #4F3603 — dark ink, unreadable on the light-mode dark-gold plate |
| `src/styles/theme.css` | 428 | theme.css already documents this exact failure class: colour NAMES (bg-black/text-white) were missed by the hex sweep, leaving dark ink on black grounds |
| `src/config/contrast.selfcheck.mjs` | 69 | the audit only pairs 'gold-ink' with 'gold' (also L134); no pair exists for accent-ink-on-gold or ink-dim-on-gold, and hardcoded #1a1a1a/black/40 plates are not tokens so they can never enter the list |

### Smallest fix

Two edits, both in src/StockOpnameView.jsx, plus one guard. (1) In the monitor-grid card (the `monitorStats.map` block, lines 671-731) replace the five hardcoded plates — `bg-[#1a1a1a]`/`border-[#2a2a2a]` (L671), `bg-black/40` (L682), `border-[#333]` (L683), `bg-black/60` (L693), `bg-[#111]` (L714/L719/L724/L731) — with the surface tokens `--panel`/`--raised`/`--inset`/`--sunk` and `--line`, and drop the duplicate border-colour class on L682/L683/L731. (2) Everywhere `bg-[var(--gold)]` is a plate under text, change the ink to `text-[var(--gold-ink)]`: the HEALTHY chip (L713), the SUNK CAPITAL wrapper's two children (L790, L791), the icon puck (L809), CONVERT TO SAMPLE (L825), RTV FACTORY (L828) and its hover, and the SAMPLING branch of the modal submit (L604) — or, if the gold fill is not wanted there, swap the plate to `--raised`/`--sunk` and keep `--accent-ink`. (3) In src/config/contrast.selfcheck.mjs add the missing pairs `['accent-ink','gold']` and `['ink-dim','gold']` so the parent-plate/child-ink shape fails the audit instead of shipping.

### Could not check

I did not run the app or measure contrast in a browser — all ratios above are computed from the hex values in theme.css, not from tools/theme-lab.html. I could not verify the user's claim that Screen A is unreadable specifically in DARK mode: in dark, --ink (#E8E4DE) on #1a1a1a is nominally high-contrast, so either the reporter was actually in light mode (where the hardcoded black plates are unambiguously the defect), or something above StockOpnameView forces a theme scope I did not trace. I also did not verify how the app root sets the theme (data-theme attribute vs class) or whether any wrapper exempts this screen from the theme — there is a PROGRESS.md note "A SCREEN CAN BE EXEMPT FROM THE THEME" (fdc9bbb) that I did not follow up. Tailwind arbitrary-value class ordering: I assumed the duplicate border-colour classes on L682/L683/L731 resolve by generated-CSS order, but I did not inspect the built CSS to confirm which wins. I read only the monitor and quarantine blocks of StockOpnameView.jsx (roughly L560-845), not the 'count' and 'review' view modes or the liquidation-history list below L840, so there may be more of the same shape there.


---

## ?

**Verdict:** Both jobs are real and tractable: the app genuinely shows two different words for the same thing ("Consignment" on the map/agent screens, "Titip" on the sales terminal and history), because the stored `paymentType` value is literally the string `'Titip'` and only SOME render sites map it to "Consignment" via an inline ternary — the rest print the raw stored value. And MapMissionControl's customer panel (StoreBottomSheet) shows no IOU at all today, but the IOU data (`pendingIOUs`) already rides along on the `store` object, so no new data source is needed.

### Mechanism

(a) LANGUAGE SPLIT. The database stores paymentType === 'Titip' (written at src/MerchantSalesView.jsx:1002, defaulted in src/App.jsx:307 and src/App.jsx:1455 and src/hooks/useDatabaseSync.js:132). Three kinds of render site exist. (1) Sites with an inline translation ternary that already say "Consignment": src/MerchantSalesView.jsx:1795 (`method === 'Titip' ? 'Consignment' : method`) and src/FleetCanvasManager.jsx:778, plus src/FleetCanvasManager.jsx:974 which says the shorter "Consign". (2) Sites with a hardcoded "Titip" label: src/MerchantSalesView.jsx:1817 ("OVERDUE TITIP DETECTED" / "Active Titip Balance") and src/MerchantSalesView.jsx:1678 ("Collect payment before issuing new Titip!"). (3) Sites that print the RAW stored value with no literal to edit at all — src/AgentInventoryView.jsx:361, src/components/HistoryReportView.jsx:587 (`{t.paymentType || 'Cash'}`), :958, :1053, :1130, src/ConsignmentFinanceView.jsx:387, :465, src/FleetCanvasManager.jsx:429, :511. Category (3) is why the word "Titip" keeps reappearing no matter how many literals get edited. There is already exactly one shared display-mapper for stored payment values: `paymentLabel` at src/utils/helpers.js:248 (currently only maps 'IOU Fulfillment' -> 'Utang Barang Lunas'), imported at src/MerchantSalesView.jsx:6 and used at :1190. Adding a 'Titip' -> chosen-word case there, and routing the category-(3) sites through it, fixes all of them at once.

FULL USER-FACING LABEL INVENTORY (safe to reword):
- src/MapMissionControl.jsx:1471 — "Active Consignment" — map store pin panel (this is the one in the report).
- src/AgentProfileView.jsx:1167 — "Consignment Risk (Titip)" — agent profile card. Both words in one label.
- src/AgentProfileView.jsx:1177 — "No active consignment targets." — same card, empty state.
- src/AgentProfileView.jsx:1287 — Recharts `name="Titip (Consign)"` — chart legend/tooltip text (the `dataKey="titip"` on the same line is an identifier, do NOT touch).
- src/AgentProfileView.jsx:1293 — legend chip "Consignment".
- src/MerchantSalesView.jsx:1795 — payment-method dropdown option, "Consignment".
- src/MerchantSalesView.jsx:1817 — "⚠️ OVERDUE TITIP DETECTED" / "Active Titip Balance" — sales terminal.
- src/MerchantSalesView.jsx:1678 — "Collect payment before issuing new Titip!" — sales terminal overdue banner.
- src/FleetCanvasManager.jsx:778 — "Consignment" option label.
- src/FleetCanvasManager.jsx:974 — "Consign" chip.
- src/ConsignmentFinanceView.jsx:775 — "Active Consignments" section header.
- src/ConsignmentFinanceView.jsx:853 — column header "Dititip".
- src/ConsignmentFinanceView.jsx:881 — mobile row label "Dititip:".
- src/ConsignmentFinanceView.jsx:252 — notify() text "...total barang yang dititipkan." (Indonesian).
- src/components/BiohazardTheme.jsx:359 — nav label 'Receivables & Consignment'.
- src/components/SettingsView.jsx:1436 — permission-matrix label 'Receivables & Consign'.
- src/components/HistoryReportView.jsx:995 — "Total Consigned:" — this is inside the printed receipt block (receipt keeps company theme, but the word is still a label).
- src/components/HistoryReportView.jsx:818 — the "(CONSIGNMENT)" tag rendered when paymentType === 'Titip'.

CODE IDENTIFIERS — MUST NOT BE RENAMED:
- Transaction type constant 'CONSIGNMENT_PAYMENT': written at src/hooks/useTransactionEngine.js:461, :538, :560; read at src/AgentInventoryView.jsx:81, src/AgentProfileView.jsx:519, src/App.jsx:2699, src/components/HistoryReportView.jsx:576/796/803/936/948/1041, src/ConsignmentFinanceView.jsx:104/203, src/EODReconciliationView.jsx:190/215, src/FleetCanvasManager.jsx:533, src/MapMissionControl.jsx:1165/1181, src/MerchantSalesView.jsx:122, plus src/config/logicFixes.selfcheck.mjs:310/320/429/436/528/535/852/863/875.
- Stored paymentType VALUE 'Titip': src/MerchantSalesView.jsx:1002/1799, src/App.jsx:307/1455/2699, src/hooks/useDatabaseSync.js:132, src/AgentProfileView.jsx:489/505/508, src/ConsignmentFinanceView.jsx:94/177, src/EODReconciliationView.jsx:190, src/MapMissionControl.jsx:1164/1172, src/MerchantSalesView.jsx:110, src/components/HistoryReportView.jsx:259/587/818/819, src/FleetCanvasManager.jsx:775/974. The allowedPayments arrays store this value too.
- Career/EOD stat key `titipCollected`: src/config/career.js:25/62/115, src/App.jsx:2025/2066/2131/2136/2154, src/EODReconciliationView.jsx:214/264/674, src/components/AchievementTester.jsx:25, src/AgentProfileView.jsx:520/601.
- Symbol names: ConsignmentFinanceView (file + src/App.jsx:45/4371), handleConsignmentPayment / handleConsignmentReturn (src/hooks/useTransactionEngine.js:429/570/599/600, src/App.jsx:3054/4374/4375), handleDeleteConsignmentData (src/App.jsx:2696/4377), and local vars totalTitip / currentConsignment / showConsignDetails / activeTitipResponsibility / isTitip / showTitipBreakdown.
- Permission feature id 'view_receivables' (src/components/BiohazardTheme.jsx:359, src/components/SettingsView.jsx:1436) — the id is an identifier, only its `label` is a label.
- Note: src/config/integration.audit.mjs:1449 mentions "Receivables & Consignment" only in a COMMENT about label width; no audit needle asserts any consignment/titip string, so renaming labels breaks no self-check.

(b) MAP CUSTOMER PANEL. The panel opened by pressing a store pin is `StoreBottomSheet`, defined at src/MapMissionControl.jsx:1058 and rendered at src/MapMissionControl.jsx:2599 with `store={activeStore}` (activeStore resolved at :2176). What it currently shows: store name (:1341), Wholesaler badge (:1345), address/city/region line (:1348 via displayLocation :1313), Directions link (:1356), WhatsApp or "No Phone" (:1361), Correct Pin / Edit Profile when canOverrideGps (:1376, :1384), a "Next Visit Target" due/overdue block with an admin visit-frequency input (:1399), and then — ALL behind an `isAdmin &&` gate that opens just after the visit block and closes near :1519 — Override Performance Tier select (:1426), Delete (:1345ff), Individual Reach scale slider (:1443), Set as Wholesale Hub toggle (:1453), Map to Wholesaler select (:1459), the "Active Consignment" card with expandable per-item Bks list (:1468-1481, fed by the `stats` memo at :1150-1193), and "Recent Sales" with lifetime revenue (:1486, fed by `recentSales` at :1195). A non-admin sees none of the money blocks.

IOU DATA. Field name: `pendingIOUs` — an ARRAY on the CUSTOMER document at `artifacts/{appId}/users/{userId}/customers/{customerId}`. Read/written at src/MerchantSalesView.jsx:1026 and :1029; each element is `{ id, productId, name, qty, unit, date, agentName }`, shaped at src/MerchantSalesView.jsx:1014-1018. It is not a rupiah balance — it is a list of goods owed to the store (Utang Barang), generated only by EXCHANGE returns where the item's `fulfillment === 'IOU'`.

EXISTING HELPER THAT COMPUTES OUTSTANDING IOU: none. There is no shared function anywhere; the only consumer renders the array directly (banner at src/MerchantSalesView.jsx:1649-1665, "Utang Barang Belum Dikirim") and the only shared IOU-related helper is the label mapper `paymentLabel` at src/utils/helpers.js:248. Outstanding IOU = `store.pendingIOUs.length` / the array itself; there is nothing to reuse and nothing that needs writing.

IS THE DATA ALREADY IN MapMissionControl? YES — no new source needed. App.jsx:4062 passes `customers={userRole === 'ADMIN' ? displayCustomers : displayPermitted}`; both are `(list).map(c => ({ ...c, name: storeLabel(c.name) }))` (src/App.jsx:3050-3051 and :3559-3560), so the full raw customer doc survives. MapMissionControl's `mapPoints` memo (src/MapMissionControl.jsx:1984-2048) returns `{ ...c, ... }` at :2036, again preserving every field. So `store.pendingIOUs` is already readable inside StoreBottomSheet with zero prop or query changes.

### Citations

| file | line | what |
|---|---|---|
| `src/MapMissionControl.jsx` | 1471 | "Active Consignment" label in the map store panel — the exact string in the report |
| `src/MerchantSalesView.jsx` | 1817 | Sales terminal: 'OVERDUE TITIP DETECTED' / 'Active Titip Balance' — the 'titip' side of the mismatch |
| `src/MerchantSalesView.jsx` | 1795 | Terminal dropdown already translates stored 'Titip' to the word 'Consignment' via an inline ternary |
| `src/utils/helpers.js` | 248 | paymentLabel — the only existing shared display-mapper for stored payment values; the single hook for a rename |
| `src/components/HistoryReportView.jsx` | 587 | Renders {t.paymentType || 'Cash'} raw — prints the stored word 'Titip' with no literal to edit |
| `src/hooks/useTransactionEngine.js` | 461 | 'CONSIGNMENT_PAYMENT' transaction type written to Firestore — code identifier, must not be renamed |
| `src/MerchantSalesView.jsx` | 1002 | paymentType stored as the literal 'Titip' on the sale document |
| `src/MapMissionControl.jsx` | 1058 | StoreBottomSheet — the customer panel shown when a store pin is pressed |
| `src/MapMissionControl.jsx` | 1150 | stats memo: computes currentConsignment = totalTitip - totalPaid and the activeItems list for that panel |
| `src/MapMissionControl.jsx` | 2036 | mapPoints spreads the whole customer doc ({...c}), so pendingIOUs reaches the panel already |
| `src/App.jsx` | 4062 | MapMissionControl props — customers comes from displayCustomers/displayPermitted, no IOU-specific prop |
| `src/App.jsx` | 3050 | displayCustomers is a {...c} spread — pendingIOUs survives |
| `src/MerchantSalesView.jsx` | 1026 | pendingIOUs read from the customer document at artifacts/{appId}/users/{userId}/customers/{id} |
| `src/MerchantSalesView.jsx` | 1014 | IOU element shape: {id, productId, name, qty, unit, date, agentName} |
| `src/MerchantSalesView.jsx` | 1649 | The only existing IOU UI: 'Utang Barang Belum Dikirim' banner, rendered straight off the array |
| `src/config/integration.audit.mjs` | 1449 | 'Receivables & Consignment' appears only in a comment about label width; no audit needle asserts consignment/titip strings |

### Smallest fix

(a) Pick one word, then: extend `paymentLabel` in src/utils/helpers.js to map 'Titip' to it, and route the raw-value render sites through it (src/components/HistoryReportView.jsx:587/958/1053/1130, src/AgentInventoryView.jsx:361, src/ConsignmentFinanceView.jsx:387/465, src/FleetCanvasManager.jsx:429/511) instead of adding more inline ternaries; then reword the ~18 hardcoded labels listed above, replacing the inline ternaries at src/MerchantSalesView.jsx:1795 and src/FleetCanvasManager.jsx:778/974 with paymentLabel calls. Change no 'Titip' or 'CONSIGNMENT_PAYMENT' comparison, no `titipCollected` key, no symbol name.
(b) In `StoreBottomSheet` (src/MapMissionControl.jsx:1058), read `store.pendingIOUs` directly and render a card beside the "Active Consignment" block at src/MapMissionControl.jsx:1468 — mirroring the existing banner markup at src/MerchantSalesView.jsx:1649. No new prop, no new query, no new helper. One decision needed: the existing consignment/sales blocks sit inside the `isAdmin &&` gate that closes near src/MapMissionControl.jsx:1519, so placing the IOU card there hides it from salesmen; placing it above that gate (next to the Next Visit block at :1394) shows it to everyone.

### Could not check

1) I did not run the app, so I could not confirm visually which of these labels Aldi actually saw side by side. 2) I could not verify against live Firestore whether any legacy customer documents store `pendingIOUs` under a different key or whether any sale documents store a paymentType spelled other than exactly 'Titip'. 3) Whether "Consignment" or "Titip" is the word to keep is Aldi's call — I inventoried both sides rather than assuming. 4) I did not audit non-src assets (public/, docs/merchant/*.md, coin_flip.html) for the words; the grep covered src/, firestore.rules and index.html only. 5) firestore.rules was not checked for a rule that constrains the `pendingIOUs` field shape on customer writes.


---

## ?

**Verdict:** Real on all counts: "Reconcile & Clear" has no tier gate at all (its only render condition is that the vehicle is non-empty), it returns only unsold canvas stock, and the red rows on Fleet & Roster are computed from today's SALE/RETUR transactions, not from the canvas — so nothing in either Reconcile & Clear or handleVerifyEOD can ever clear them; they disappear only when the calendar day rolls over.

### Mechanism

1) RECONCILE & CLEAR. Rendered in FleetCanvasManager.jsx at line 1059, inside a block whose only condition is `(selectedAgent.activeCanvas || []).length > 0` (line 1057). There is NO role, tier, or permission term in that condition, and no guard inside the handler either — handleClearCanvas (line 356) opens with `if (!selectedAgent) return;` and nothing else. Any user who can reach the `fleet` tab and select an agent sees and can fire it. Tab access itself is `view_fleet`, granted to TIER_2/3/4 (permissions.js:59, 64, 68) plus TIER_1's ALL_ACCESS (permissions.js:57, 104) — so a Tier 2 owner, a Tier 3 area admin and a Tier 4 fleet captain all currently get the button. What it writes: a runTransaction that re-reads `activeCanvas` live (line 388), converts each row to Bks via convertToBks, credits either the agent's branch inventory doc or the master `products` doc depending on isFieldLevelTier(selectedAgent.userRole) (lines 361-364, 396-411), then `t.update(agentRef, { activeCanvas: [] })` (line 413). It writes only stock that is still physically in the van. 2) RED ON FLEET & ROSTER. Same component. `combinedItems` (FleetCanvasManager.jsx:448-475) seeds a map from `selectedAgent.activeCanvas` (currentBks) and then ADDS a second, independent source: `agentSales` (line 446) = every transaction with `t.agentId === selectedAgent.id && t.date === todayStr` of type SALE or RETUR, accumulated into `soldBks`, creating a map row with `currentBks: 0` for any product no longer in the canvas (line 469). The red state is decided purely by `item.currentBks > 0 ? emerald : red` — the status dot at line 1074 and the LEFT figure at line 1086. So "red" literally means "zero left in the van", not "sold" and not "owed". Nothing clears it: it is derived state with no stored flag. It vanishes only when `todayStr` (line 443, `new Date().toISOString().split('T')[0]`) advances past the transaction's `date`, dropping the row out of `agentSales`. Reconcile & Clear makes it WORSE, not better — emptying the canvas forces `currentBks` to 0 for every remaining line, so every row on the screen turns red at once. 3) THE EOD PATH. handleVerifyEOD (App.jsx:1762) reads `report.remainingStock` (App.jsx:1799) and `report.damagedStockToReturn` (App.jsx:1810) and credits them to the branch or master vault (App.jsx:1858-1863, 1885-1889), then sets `activeCanvas: []` unless the report is CUKAI (App.jsx:1998, 2000). `remainingStock` is built in EODReconciliationView.jsx:657 from `agentData.activeStock`, which is `resolvedCanvas` (line 264), which is `agentProfile.activeCanvas` (line 152). It is unsold stock only. There is no sold-goods branch anywhere in handleVerifyEOD — and structurally there cannot be one today, because a sale already removed the goods: MerchantSalesView.jsx:903 writes `activeCanvas: updatedCanvas.filter(c => c.qty > 0)`, physically deleting a fully-sold product row from the van. Sold goods are gone from inventory by design; the only sold-related thing EOD handles is the MONEY (cash/transfer variance and the PENALTY_ bounty minting at App.jsx:1990-1995). So: the remaining-stock case only. 4) TIERS. Tiers are the string `userRole` on the motorist/employee doc, values from CORPORATE_TIERS (permissions.js:2-9): TIER_1 'DEVELOPER', TIER_2 'COMPANY_OWNER', TIER_3 'AREA_ADMIN', TIER_4 'FLEET_CAPTAIN', TIER_5 'FIELD_OPERATIVE', TIER_6 'ROOKIE', with legacy tags normalised by translateLegacyRole (permissions.js:26-34 — note 'ADMIN' maps to TIER_1). The real Tier-1-only example in the codebase is the Architect tab: SettingsView.jsx:214 pushes `{ id: 'architect', label: 'Architect (Tier 1)' }` only `if (isSystemOwner)` (line 213), and the panel renders on `{activeTab === 'architect' && isSystemOwner && (` (SettingsView.jsx:1197), which is what gates CareerDevTools (SettingsView.jsx:1237, documented "Tier 1 only" at its header, CareerDevTools.jsx:10). CRITICAL: `isSystemOwner` — NOT `userRole` — is the only Tier-1 discriminator that works, because App.jsx sets `setUserRole('ADMIN')` for BOTH the Tier-1 architect (App.jsx:2230) and a Tier-2 COMPANY_OWNER (App.jsx:2284-2287). `isSystemOwner` is App.jsx state (line 188), set true only for a `system_admins` doc holder or the hardcoded masterVIPs email (App.jsx:2209-2210, 2227-2228). It is currently passed to exactly one component: SettingsView (App.jsx:4510). Existing role checks that LOOK tier-1-only are actually tier-1-or-2, e.g. StockOpnameView.jsx:508 `if (userRole !== 'DEVELOPER' && userRole !== 'COMPANY_OWNER') return;`. FleetCanvasManager's own `isGlobalAdmin` (line 15) lumps DEVELOPER, COMPANY_OWNER and ADMIN together, so no prop it currently receives (App.jsx:4068-4079: db, appId, user, userRole, inventory, transactions, appSettings, logAudit, triggerCapy, isAdmin, motorists) can tell Tier 1 from Tier 2.

### Citations

| file | line | what |
|---|---|---|
| `src/FleetCanvasManager.jsx` | 1057 | The ONLY gating condition on the Reconcile & Clear button: `{(selectedAgent.activeCanvas || []).length > 0 && (` — no role/tier/permission term |
| `src/FleetCanvasManager.jsx` | 1059 | The Reconcile & Clear button itself, onClick={handleClearCanvas} |
| `src/FleetCanvasManager.jsx` | 356 | handleClearCanvas definition; line 357 is its only guard, `if (!selectedAgent) return;` — no permission check inside the handler either |
| `src/FleetCanvasManager.jsx` | 413 | What the handler writes to the agent doc: `t.update(agentRef, { activeCanvas: [] })` |
| `src/FleetCanvasManager.jsx` | 396 | Destination credit: t.set on branch inventory or master products with currentStock + returnInBks (branch vs master decided at lines 361-364 via isFieldLevelTier) |
| `src/FleetCanvasManager.jsx` | 448 | combinedItems useMemo — merges activeCanvas rows (currentBks) with today's sales rows (soldBks) |
| `src/FleetCanvasManager.jsx` | 469 | Creates a row with currentBks: 0 for a product that is in today's sales but no longer in the canvas — this is the row that renders red |
| `src/FleetCanvasManager.jsx` | 446 | agentSales filter: `t.agentId === selectedAgent?.id && t.date === todayStr` — the only thing that ever removes a red row is todayStr advancing (defined line 443) |
| `src/FleetCanvasManager.jsx` | 1074 | Red status dot: `${item.currentBks > 0 ? 'bg-emerald-500' : 'bg-red-500'}` — the field deciding red is currentBks |
| `src/FleetCanvasManager.jsx` | 1086 | Red LEFT figure: `${item.currentBks > 0 ? 'text-emerald-400' : 'text-red-500'}` |
| `src/App.jsx` | 1762 | handleVerifyEOD definition |
| `src/App.jsx` | 1799 | `const validItems = (report.remainingStock || []).filter(item => item.qty > 0);` — the only goods handleVerifyEOD returns to the vault |
| `src/App.jsx` | 1810 | validDamagedItems from report.damagedStockToReturn — the second and last goods path; there is no sold-goods path |
| `src/App.jsx` | 1998 | `const finalCanvas = (report.reportType === 'CUKAI') ? currentCanvas : [];` written at line 2000 — EOD approval is what empties the van in the sanctioned flow |
| `src/EODReconciliationView.jsx` | 152 | resolvedCanvas = agentProfile.activeCanvas — proves remainingStock is unsold stock only |
| `src/EODReconciliationView.jsx` | 657 | `remainingStock: countedStock` in the EOD submit payload, derived from agentData.activeStock (= resolvedCanvas, line 264) |
| `src/MerchantSalesView.jsx` | 903 | `activeCanvas: updatedCanvas.filter(c => c.qty > 0)` — a sale physically deletes the fully-sold row from the van, which is why sold goods can never come back through EOD |
| `src/config/permissions.js` | 2 | CORPORATE_TIERS: the tier field is the `userRole` string; TIER_1 'DEVELOPER' through TIER_6 'ROOKIE' (lines 2-9) |
| `src/config/permissions.js` | 26 | translateLegacyRole — normalises 'ADMIN' and 'DEVELOPER' both to TIER_1, which is why userRole alone cannot separate the architect from the owner |
| `src/config/permissions.js` | 57 | TIER_1: ['ALL_ACCESS'] plus the hasClearance bypass at line 104 — Tier 1 passes every feature check, so a permission-string gate alone would not exclude Tier 2 unless view_fleet were revoked |
| `src/config/permissions.js` | 59 | TIER_2 (COMPANY_OWNER) permission list includes 'view_fleet'; TIER_3 at line 64 and TIER_4 at line 68 also include it — all three currently reach the button |
| `src/components/SettingsView.jsx` | 1197 | THE REAL TIER-1-ONLY EXAMPLE: `{activeTab === 'architect' && isSystemOwner && (` — the pattern to copy |
| `src/components/SettingsView.jsx` | 213 | `if (isSystemOwner) { navTabs.push({ id: 'architect', label: 'Architect (Tier 1)' ... }) }` (lines 213-214) — the nav half of the same gate |
| `src/App.jsx` | 188 | `const [isSystemOwner, setIsSystemOwner] = useState(false);` — the only state that distinguishes Tier 1 from Tier 2 |
| `src/App.jsx` | 2228 | setIsSystemOwner(true) for a system_admins doc holder / masterVIPs email (list at line 2209-2210) |
| `src/App.jsx` | 2287 | `setUserRole('ADMIN')` for a COMPANY_OWNER (Tier 2), identical to the architect's setUserRole('ADMIN') at line 2230 — proof userRole cannot gate Tier 1 alone |
| `src/App.jsx` | 4510 | `isSystemOwner={isSystemOwner}` — currently passed to SettingsView and nothing else |
| `src/App.jsx` | 4068 | FleetCanvasManager render site; props end at line 4079 and do NOT include isSystemOwner (nor agentProfileId, despite the component signature declaring it at FleetCanvasManager.jsx:13) |
| `src/FleetCanvasManager.jsx` | 15 | `const isGlobalAdmin = ['DEVELOPER', 'COMPANY_OWNER', 'ADMIN'].includes(userRole);` — the component's coarsest existing role check, cannot separate Tier 1 from Tier 2 |
| `firestore.rules` | 440 | match /motorists/{motoristId} — update is region/authority-scoped, not per-feature; the same rule permits a sale, an EOD verify and a Reconcile & Clear because all three write activeCanvas on this doc, so the gate has to be client-side |

### Smallest fix

Two lines, one prop. (1) In src/App.jsx, at the FleetCanvasManager render site (line 4068-4079), add `isSystemOwner={isSystemOwner}` to the prop list — the state already exists at App.jsx:188 and is already threaded to SettingsView at App.jsx:4510, so nothing new is computed. (2) In src/FleetCanvasManager.jsx, accept `isSystemOwner` in the component signature (line 13) and AND it into the render condition at line 1057, so the button block becomes gated on `isSystemOwner && (selectedAgent.activeCanvas || []).length > 0`. (3) Mirror it as an early return at the top of handleClearCanvas (src/FleetCanvasManager.jsx:357, beside the existing `if (!selectedAgent) return;`) so hiding the button is not the only defence. That is the whole hook: it excludes Tier 2 COMPANY_OWNER, Tier 3 AREA_ADMIN and Tier 4 FLEET_CAPTAIN, because isSystemOwner is true only for the system_admins/masterVIPs identity (App.jsx:2209-2228), while userRole is 'ADMIN' for the owner too (App.jsx:2287). Do NOT reach for hasClearance or a new permission string here — TIER_1's ALL_ACCESS bypass (permissions.js:104) makes a permission-string gate pass for Tier 1 but a Tier-2 owner would still pass any string an owner is granted, and the whole matrix is admin-editable from Settings, so the gate would be self-revocable. Separately note: gating the button alone does not address the red rows. Nothing today writes a "sold, pending EOD" flag — the red state is recomputed from `transactions` each render (FleetCanvasManager.jsx:446, 448), so making the red survive-until-EOD-then-clear needs a real stored signal (e.g. keying agentSales off the agent's last VERIFIED eod_report timestamp instead of `todayStr` at line 443), which is a behaviour change, not a gate.

### Could not check

(a) Whether any live account actually holds userRole exactly 'COMPANY_OWNER' when it reaches FleetCanvasManager — App.jsx:2287 rewrites the owner's userRole to 'ADMIN' at login, so in practice FleetCanvasManager's `userRole` is 'ADMIN' for both Tier 1 and Tier 2 and I could not confirm from code alone whether a 'COMPANY_OWNER' string ever survives to that component. This does not change the fix (isSystemOwner is correct either way) but it does mean any gate written against userRole would silently fail. (b) The runtime-injected permission matrix: injectDynamicPermissions (permissions.js:80) overwrites ROLE_PERMISSIONS from Firestore, so the view_fleet grants I cited at permissions.js:59/64/68 are the defaults, not necessarily what this deployment serves — I did not read the live Firestore matrix doc. (c) I did not run the app or the emulator; every claim above is static reading. (d) I did not audit the full firestore.rules motorists update block beyond line 475 (the comment block runs long); I read enough to confirm it is authority/region-scoped rather than per-feature, but I did not read the final `allow update:` expression itself.


---

## ?

**Verdict:** Real. Every display site named in the report prints the `agentName` string that was BAKED INTO the transaction document at write time, and for a tier-1 owner that string is the Google `displayName` because the write path deliberately sets the motorist id to `null` for `userRole === 'ADMIN'` whenever the sales mode is VAULT (the default), which skips the Fleet-profile lookup entirely.

### Mechanism

Login: a `COMPANY_OWNER` / tier-1 account is forced to `setUserRole('ADMIN')` + `setAgentProfileId(null)` (App.jsx:2287-2288, also 2230-2231 and 2387-2388). It therefore has NO motorist id. Write: in useTransactionEngine, `finalAgentName` is seeded from the Google account (`user?.displayName || user?.email.split('@')[0] || 'Admin'`, line 79 for sales, line 431 for consignment). It is only overwritten from the Fleet document inside `if (currentAgentProfileId)` (lines 95-101 offline, 293-302 online, 443-449, 521-524). Two lines above each of those, `currentAgentProfileId` is re-derived: `if (userRole === 'ADMIN' && adminSalesMode === 'VEHICLE') currentAgentProfileId = 'ADMIN_VEHICLE'; else if (userRole === 'ADMIN') currentAgentProfileId = null;` (lines 76-77 and 434-436; identical copy in App.jsx:3065-3067 for sampling). `adminSalesMode` defaults to 'VAULT' (App.jsx:304). So in VEHICLE mode the motorists/ADMIN_VEHICLE doc is read and 'MOBIL PAK BOS' is written; in VAULT mode the lookup is skipped and 'ALDI KURNIAWAN' is written. That is the "sometimes" in the report — it is the Boss-Car toggle, not randomness. Note the id passed to MerchantSalesView at App.jsx:4351 IS 'VAULT'/'ADMIN_VEHICLE', but the engine receives the raw `agentProfileId` (null) at App.jsx:3055 and re-derives, so the 'VAULT' string never reaches the name resolution. The bad string is then persisted onto the transaction (`agentName: finalAgentName`, lines 127, 331, 463, 540, 561) and onto new customer docs (`mappedBy: finalAgentName`, lines 157, 357, 370). Read: MapMissionControl.jsx:1510 prints `tx.agentName` raw with zero motorist lookup; ConsignmentFinanceView.jsx:175/179 copies `t.agentName` into `ownerName`, printed at :784 under `Managed by:` inside a CSS `uppercase` class — hence "ALDI KURNIAWAN". Separately, AgentProfileView.jsx:176-178 synthesizes a profile `{id:'master_owner', name:'Master Owner'}` when the motorists/master_owner doc is missing or has no `name`, rendered through an `uppercase` class at :1063 -> "MASTER OWNER". That is the second name: the one account has three identities (synthetic master_owner, the ADMIN_VEHICLE fleet doc, and the raw Google displayName) because the tier-1 login is given no motorist id at all.

### Citations

| file | line | what |
|---|---|---|
| `src/hooks/useTransactionEngine.js` | 76 | ROOT CAUSE (sales). `if (userRole === 'ADMIN' && adminSalesMode === 'VEHICLE') currentAgentProfileId = 'ADMIN_VEHICLE'; else if (userRole === 'ADMIN') currentAgentProfileId = null;` — the null branch is what skips the Fleet lookup for the tier-1 owner in VAULT mode. |
| `src/hooks/useTransactionEngine.js` | 79 | `let finalAgentName = user?.displayName || user?.email?.split('@')[0] || 'Admin';` — Google-account seed for the SALE path. Survives untouched when currentAgentProfileId is null. |
| `src/hooks/useTransactionEngine.js` | 95 | Offline sale: motorist-name override is gated on `if (currentAgentProfileId)`, reading motorists/{id}.name (line 99). Skipped when null. |
| `src/hooks/useTransactionEngine.js` | 293 | Online sale: same gate `if (currentAgentProfileId)`, `finalAgentName = agentDoc.data().name` at line 300. Skipped when null. |
| `src/hooks/useTransactionEngine.js` | 127 | STORED-AT-WRITE: offline SALE payload writes `agentName: finalAgentName` next to `agentId: currentAgentProfileId || 'ADMIN'` (line 126). |
| `src/hooks/useTransactionEngine.js` | 331 | STORED-AT-WRITE: online SALE batch.set writes `agentName: finalAgentName`, `agentId: currentAgentProfileId || 'ADMIN'` (line 330). This is the document the map panel later prints. |
| `src/hooks/useTransactionEngine.js` | 434 | ROOT CAUSE (consignment). Same ADMIN->null derivation inside handleConsignmentPayment; seed at line 431. |
| `src/hooks/useTransactionEngine.js` | 463 | STORED-AT-WRITE: offline consignment/audit payload `agentName: finalAgentName`. |
| `src/hooks/useTransactionEngine.js` | 521 | Online consignment: `if (currentAgentProfileId && agentDoc?.exists() && agentDoc.data().name) finalAgentName = agentDoc.data().name;` — gated the same way. |
| `src/hooks/useTransactionEngine.js` | 540 | STORED-AT-WRITE: online CONSIGNMENT_PAYMENT batch.set writes `agentName: finalAgentName` with `agentId: currentAgentProfileId || 'ADMIN'` (line 539). This feeds the titip 'Managed by' line. |
| `src/hooks/useTransactionEngine.js` | 561 | STORED-AT-WRITE: the object returned to the caller also carries `agentName: finalAgentName` (used for the immediate on-screen receipt). |
| `src/hooks/useTransactionEngine.js` | 157 | STORED-AT-WRITE on the CUSTOMER doc: `mappedBy: finalAgentName` (offline new-store). Same defect, different collection. Also lines 357 and 370 (online NOO and walk-in). |
| `src/hooks/useTransactionEngine.js` | 205 | The engine returns `finalAgentName` to MerchantSalesView, which is where its `agentFallback` comes from. Also line 397. |
| `src/MapMissionControl.jsx` | 1510 | DISPLAY, RESOLVED-AT-READ = NO. The recent-sales panel prints `{tx.agentName === 'Admin' ? 'Admin' : (tx.agentName || 'Sales')}` — the stored string, no motorist lookup anywhere. |
| `src/MapMissionControl.jsx` | 1195 | `recentSales` useMemo — filters and sorts transactions only; performs no name resolution, so nothing upstream of line 1510 could repair the name. |
| `src/ConsignmentFinanceView.jsx` | 175 | `ownerName: t.agentName || 'Admin'` — the titip 'Managed by' value is taken straight off the stored transaction. |
| `src/ConsignmentFinanceView.jsx` | 179 | `customers[name].ownerName = t.agentName || 'Admin';` — overwritten by the latest Titip SALE, still from the stored string. |
| `src/ConsignmentFinanceView.jsx` | 784 | DISPLAY: `Managed by: {c.ownerName}` inside a class list containing `uppercase` — this is the literal 'titip managed by ALDI KURNIAWAN' in the report. |
| `src/ConsignmentFinanceView.jsx` | 60 | Related smell: the agent-scoping filter matches on `t.agentName === user.displayName || user.name || user.email.split('@')[0]` — name-string matching against the Google account, not agentId. |
| `src/App.jsx` | 2287 | IDENTITY ROOT: `if (activeData.role === 'COMPANY_OWNER') { setUserRole('ADMIN'); setAgentProfileId(null); }` — the tier-1 owner is given no motorist id, which is why every ADMIN-branch lookup above has nothing to read. Same at lines 2230-2231 and 2387-2388. |
| `src/App.jsx` | 2344 | CONTRAST: an EMPLOYEE gets `setUserRole(finalUserRole); setAgentProfileId(trueAgentId);` and their `displayName` is overwritten with the Fleet name at line 2317/2350 — which is why only the owner sees the Google name. |
| `src/App.jsx` | 304 | `adminSalesMode` defaults to 'VAULT' — so the default owner sale takes the null branch and writes the Google name. |
| `src/App.jsx` | 4351 | `agentProfileId={userRole === 'ADMIN' ? (adminSalesMode === 'VEHICLE' ? 'ADMIN_VEHICLE' : 'VAULT') : agentProfileId}` — this corrected id goes only to MerchantSalesView's UI, NOT to the engine. |
| `src/App.jsx` | 3055 | The engine is constructed with the RAW `agentProfileId` (null for the owner), which is why the 'VAULT' string at 4351 never reaches name resolution. |
| `src/App.jsx` | 3065 | Fourth copy of the same ADMIN->null derivation, in handleBatchSamplingSubmit. |
| `src/App.jsx` | 1727 | STORED-AT-WRITE, worst offender: `const formattedAgentName = `${user.displayName || "Field Agent"} - ${user.email || "No Email"}`;` — pure Google account, no motorist lookup at all. |
| `src/App.jsx` | 1735 | That string is written into eod_reports as `agentName: formattedAgentName`, with the CORRECT id `effectiveAgentId` ('ADMIN_VEHICLE'/'VAULT') computed right above at line 1731 — the id is right, the name is wrong. |
| `src/App.jsx` | 1656 | Existing mass-rename precedent: territory transfer rewrites historical transactions with `data: { agentId: request.toAgentId, agentName: request.toAgentName }` — proof that a batch agentName migration is already an established operation in this codebase. |
| `src/App.jsx` | 1489 | SECOND CONTAMINATION PATH: the telemetry ticker does setDoc(merge) on motorists/{activeTrackerId} with `name: activeTrackerId === 'master_owner' ? 'Master HQ' : (user.displayName || 'Agent')` — for any non-master tracker id this OVERWRITES the Fleet-registered name with the Google displayName inside the motorist document itself. |
| `src/App.jsx` | 1468 | `const activeTrackerId = user?.agentId || (user?.role === 'COMPANY_OWNER' || user?.role === 'ADMIN' || user?.tier === 1 ? 'master_owner' : null);` — origin of the master_owner id for a tier-1 account. |
| `src/AgentProfileView.jsx` | 177 | 'MASTER OWNER' ORIGIN: `setOwnerProfile({id:'master_owner', userRole:'COMPANY_OWNER', name:'Master Owner', ...snap.data()})` — the literal default name. Line 178 is the same default when the doc does not exist at all. |
| `src/AgentProfileView.jsx` | 1063 | `<h1 className="... uppercase ...">{activeAgent.name}</h1>` — renders 'Master Owner' as 'MASTER OWNER'. This is the second name the report complains about. |
| `src/AgentProfileView.jsx` | 187 | `if (ownerProfile && !list.find(m => m.id === 'master_owner')) list.unshift(ownerProfile);` — the synthetic profile is injected into the roster alongside the real Fleet entries, so both names appear in one list. |
| `src/AgentProfileView.jsx` | 454 | Symptom of the same split identity: ownership of a transaction is decided by `t.agentId === activeAgent.id || t.agentName.toLowerCase() === activeAgent.name?.toLowerCase()` — name-string fallback. |
| `src/components/HistoryReportView.jsx` | 132 | THE ONE RESOLVED-AT-READ SITE, and the pattern to copy: starts from `t.agentName` then REPLACES it. |
| `src/components/HistoryReportView.jsx` | 136 | For `agentId === 'ADMIN'` it finds the boss motorist (`m.role === 'COMPANY_OWNER' || m.id === 'master_owner' || m.id === 'ADMIN'`) and uses `boss.name` (line 139) — proving the owner's stored-wrong rows CAN be repaired at read time from agentId alone. |
| `src/components/HistoryReportView.jsx` | 147 | For a normal agent: `agentName = motorist.name || motorist.agentName || agentName;` — live Fleet lookup by agentId. |
| `src/components/HistoryReportView.jsx` | 1052 | But the RECEIPT inside the same file is stored-at-read-of-stored: `viewingReceipt.agentName === 'Admin' ? (appSettings?.adminDisplayName || 'Admin') : (viewingReceipt.agentName || 'Sales')` — no motorist lookup. Same at line 957. |
| `src/EODReconciliationView.jsx` | 333 | `resolveIdentityName()` = `motorists.find(m => m.id === effectiveId)?.name` — CORRECT source (Fleet), but its output is then STORED into eod_reports at lines 463, 670, 693, 827. |
| `src/EODReconciliationView.jsx` | 871 | DISPLAY of the stored value: `{report.agentName}`. Also line 315 (`report.agentName || 'Unknown Agent'`) and lines 894, and App.jsx:1775/1777/1778/1907/2084/2090/2094 print the same stored string in dialogs and audit logs. |
| `src/MerchantSalesView.jsx` | 1012 | `const agentFallback = typeof trueAgentName === 'string' ? trueAgentName : (user?.displayName || ...)` — takes the engine's finalAgentName, else the Google account again. |
| `src/MerchantSalesView.jsx` | 1017 | STORED-AT-WRITE: IOU records get `agentName: agentFallback`. Displayed at line 1656 (`By: {iou.agentName}`). |
| `src/MerchantSalesView.jsx` | 1177 | STORED-AT-WRITE on the customer doc: `lastVisitedBy: agentFallback`. Also line 1182 for the local cache, and line 1191 for a second agentName-bearing record. |
| `src/MerchantSalesView.jsx` | 3088 | DISPLAY of stored `receiptData.agentName`; also lines 3025 and 3146. Mirrored in ConsignmentFinanceView.jsx:386/464/536 and FleetCanvasManager.jsx:510. |
| `src/components/DashboardView.jsx` | 45 | DISPLAY/aggregation keyed on the stored string: `const agent = t.agentName || 'Admin / Unknown';` — leaderboard rows split under two spellings of one person. |
| `src/JourneyView.jsx` | 266 | DISPLAY/aggregation on stored `t.agentName`; line 280 keys visit ownership by the same string; line 1176 and 1502 print `metric.agentName.split(' ')[0]`. |
| `src/AgentInventoryView.jsx` | 33 | RESOLVED-AT-READ (correct pattern, header only): `liveProfileData?.name || safeAgentProfile?.name || user?.displayName || ...` — Fleet name first, Google account last. |

### Smallest fix

One change fixes all FUTURE writes: in `src/hooks/useTransactionEngine.js`, the ADMIN branch at line 77 (and its identical twin at line 436, plus the copy in `handleBatchSamplingSubmit` at App.jsx:3067) must not set `currentAgentProfileId = null`. Give the owner a real Fleet id in VAULT mode — the same one the UI is already told to use at App.jsx:4351 — so the existing `if (currentAgentProfileId)` lookups at lines 95/293/443/521 actually run. Because the ADMIN-null path is shared by every write site in that file, this single derivation is the root-cause point; patching the map panel or the consignment view alone leaves sales, consignment, returns, `mappedBy`, IOUs and `lastVisitedBy` all still writing the Google name. Then, separately, `handleSubmitEOD` in App.jsx:1727 must stop building `formattedAgentName` from `user.displayName`/`user.email` and use the same Fleet resolution (its `effectiveAgentId` at line 1731 is already correct). For HISTORICAL documents no migration is strictly required: every affected transaction already carries a usable `agentId` ('ADMIN', 'ADMIN_VEHICLE' or a motorist id, written at useTransactionEngine.js lines 126/330/539), and `HistoryReportView.jsx:132-148` already contains a working agentId-to-motorist-name resolver including the `agentId === 'ADMIN'` -> boss-motorist case. Lift that block into a shared helper (`src/utils/helpers.js` is the existing home for `storeLabel`/`storeKey`) and call it from `MapMissionControl.jsx:1510` and `ConsignmentFinanceView.jsx:175/179`, and the old rows display correctly with no data rewrite. For the two-names problem, `AgentProfileView.jsx:177-178` should stop synthesizing `name: 'Master Owner'` and instead read the owner's real Fleet name; and `App.jsx:1489` should stop writing `name` into the motorist doc at all, since telemetry has no business setting a person's name.

### Could not check

(1) Live Firestore data — I could not confirm that `motorists/ADMIN_VEHICLE` actually holds the name 'MOBIL PAK BOS', nor whether `motorists/master_owner` exists and what its `name` field currently is ('Master HQ' from the telemetry write at App.jsx:1489, versus absent leaving the 'Master Owner' default at AgentProfileView.jsx:177). This determines whether "MASTER OWNER" is the synthesized default or stored data. (2) No code in `src/FleetCanvasManager.jsx` creates a document with the literal id 'ADMIN_VEHICLE' — its writes at lines 183/232/247 use ids I did not trace to that constant, so I cannot say from source alone how the Boss Car motorist doc comes into existence. (3) I did not verify whether Aldi's affected historical rows carry `agentId: 'ADMIN'` or `agentId: 'ADMIN_VEHICLE'`; my claim that a read-time fix avoids migration depends on that field being present and correct on the old documents, which I inferred from the write sites (lines 126/330/539) rather than observed in data. Legacy rows predating those lines may have no `agentId` at all, and those WOULD need migration. (4) I did not check `functions/` or `firestore.rules` for any server-side name stamping. (5) I did not inspect `dist/` (build output) — findings are from `src/` only.


---

## ?

**Verdict:** The report is accurate in effect but not in cause: the New Count panel DOES have the system-expected quantity in scope and does render it — but only after the counter types a number (a deliberate "blind count" reveal), and the number it prints (`item.stock`) is not the number the variance is computed against (`item.stock + item.damagedStock`), so even after the reveal the comparison on screen does not add up.

=== 1. COMPONENT AND TABS ===
Everything is one file, one component: `StockOpnameView` (src/StockOpnameView.jsx:14). Lazy-imported at src/App.jsx:40, mounted at src/App.jsx:4439-4452 when `activeTab === 'stock_opname'`.
Props passed (App.jsx:4441-4451): inventory, db, storage, appId, user={...user, userRole}, isAdmin, logAudit, triggerCapy, motorists, transactions, appSettings.
Nav entry: src/components/BiohazardTheme.jsx:361 `{ id:'stock_opname', label:'Stock Opname', feature:'view_stock_opname' }`. Permission key granted to TIER_2 at src/config/permissions.js:59; per-tier toggle at src/components/SettingsView.jsx:1438.

Tabs are a single `viewMode` state (StockOpnameView.jsx:34), initial `isHighCommand ? 'monitor' : 'count'`:
- 'monitor'  -> MONITOR (render block L652-750)
- 'review'   -> HQ AUDITS (render block L885-1050)
- 'quarantine' -> QUARANTINE (render block L755-880)
- 'count'    -> NEW COUNT (render block L1055-1125)
The tab bar itself (L631-646) is rendered ONLY `if (isHighCommand)`. `isHighCommand = userRole === 'ADMIN'` (L27); `isAreaAdmin = !isHighCommand` (L30). So every non-ADMIN sees no tab bar at all and is locked into NEW COUNT.
Secondary state: `auditSubTab` 'pending'|'history' (L35), `quarSubTab` 'active'|'history' (L36), `regionFilter` (L37), `monitorFacility` (L38), `quarantineFacility` (L64).
`masterId = user?.bossUid || user?.uid || user?.id` (L32) — all paths hang off `artifacts/{appId}/users/{masterId}/`.

=== 2. THE DOCUMENT A STOCK COUNT WRITES ===
Handler `handleCommit` (L201-260). Written via `addDoc` at L239 to:
  artifacts/{appId}/users/{masterId}/pending_audits/{auto-id}
Payload (L208-237), top level:
- agentId: string — `user.agentId || 'VAULT'` (L214)
- agentName: string — `user.displayName || user.email.split('@')[0] || "Branch Admin"` (L215)
- auditType: string — 'BRANCH_WAREHOUSE' if isAreaAdmin else 'MASTER_VAULT' (L216)
- branchLocation: string — `user.location || 'HQ'` (L217)
- timestamp: serverTimestamp (L218)
- status: string — 'PENDING_HQ_APPROVAL' (L219)
- items: array (L220-236), each element:
  - productId: string (item.id)
  - name: string
  - expectedStock: number — snapshot of `item.stock || 0` AT COUNT TIME (L228)
  - expectedDamagedStock: number — snapshot of `item.damagedStock || 0` (L229)
  - goodCount: number (L230)
  - damagedCount: number (L231)
  - totalFound: number = good + damaged (L232)
  - variance: number = totalFound - (item.stock + item.damagedStock) (L233)
  - damagedPhotoUrl: string|null (L234)
Fields added LATER by resolution (not present at create):
- status -> 'APPROVED' (L309) or 'REJECTED' (L327)
- resolvedAt: serverTimestamp (L309, L327)
- resolvedBy: string, email prefix (L309, L327)
- rejectReason: string — reject only, `reason || "Discrepancy too high."` (L327)
Side effects of submit:
- notification doc to artifacts/{appId}/users/{masterId}/notifications (L242-251): title, message, type:'AUDIT_PENDING', read:false, isRead:false, timestamp, agentId:'ADMIN', linkToTab:'stock_opname'
- logAudit("STOCK_OPNAME_SUBMITTED", ...) (L253); triggerCapy (L254); `setCounts({})` (L256)
Damage photo: `handlePhotoUpload` (L167-180) -> compressImageToBase64 -> savePhotoAndGetReference to `artifacts/{appId}/users/{masterId}/photos/stockopname_{productId}_{Date.now()}.jpg` (L172-173), gated on `appSettings.usePhotoStorage`. Cleared by `handleClearPhoto` (L182-186).
Rules: firestore.rules:696-700 — `read: isAreaAdmin(bossUid)`, `create: isSalesman(bossUid)`, `update, delete: false` (ADMIN gets full access via the users/{bossUid} catch-all).

=== 3. HOW A COUNT IS APPROVED ===
`handleApproveAudit(audit)` (L262-318), triggered from the expanded audit card button "Approve & Quarantine Damages" (L1037-1039).
- confirmAction gate (L265).
- basePath (L273-275): auditType 'BRANCH_WAREHOUSE' -> `.../branches/{audit.branchLocation}/inventory`; otherwise -> `.../products`.
- Per item (L277-306): `hasSnapshot = item.expectedStock !== undefined && item.expectedDamagedStock !== undefined` (L292).
  - hasSnapshot -> `{ stock: increment(goodCount - expectedStock), damagedStock: increment(damagedCount - expectedDamagedStock) }` (L294-295) — applies the DIFFERENCE, not the counted number, so sales/returns since the count survive.
  - legacy (no snapshot) -> `{ stock: goodCount, damagedStock: damagedCount }` overwrite (L299).
  - BRANCH -> op type 'set' with `{merge:true}` (L302); MASTER -> op type 'update' (L304).
- Audit doc op: `{ status:'APPROVED', resolvedAt: serverTimestamp(), resolvedBy: user.email.split('@')[0] }` (L309).
- All ops committed via `commitInChunks(db, writeBatch, operations)` (L311); logAudit("STOCK_OPNAME_APPROVED") (L312).
IMPORTANT: approval writes NOTHING to any quarantine collection despite the button label. "Quarantine" is purely a derived view over `damagedStock > 0` — see section 5.
Reject: `handleRejectAudit` (L320-332) — promptAction for reason (L321), single `updateDoc` setting status/rejectReason/resolvedAt/resolvedBy (L327). No inventory write at all.

=== 4. HQ AUDITS / AUDIT HISTORY — WHAT IT READS AND DISPLAYS ===
Listener (L80-85, only when isHighCommand) on `artifacts/{appId}/users/{masterId}/pending_audits`, split client-side:
- pendingAudits = status 'PENDING_HQ_APPROVAL', sorted by timestamp.seconds desc (L83)
- auditHistory = status 'APPROVED' or 'REJECTED', sorted by resolvedAt.seconds desc (L84)
`displayedAudits` (L531-536) picks the sub-tab source then applies regionFilter: 'ALL' = all; 'MASTER' = branchLocation 'HQ' or 'MASTER_VAULT'; otherwise branchLocation === filter.
Per-row derived values (L920-928): `totalDamaged` = sum of item.damagedCount>0; `purelyMissing` = sum of |item.variance| where variance<0; `hasIssues` = either > 0.
Collapsed row (L935-969) shows: status icon (L938), `audit.branchLocation` (L943), status badge in history (L946-948), "Count By: {agentName.toUpperCase()} • {timestamp}" (L952), "Resolved By: {resolvedBy} • {resolvedAt}" in history (L954-957), and on the right one of: "{purelyMissing} Bks Missing" (L963), "{totalDamaged} Bks Damaged" (L964), "PERFECT MATCH" (L965).
Expanded (L971-1042): rejection reason box (L974-979); then per item: name, "SYS: {item.expectedStock}", arrow, "FND: {item.totalFound}", signed variance (L990-997); "Good Condition: {goodCount} Bks" (L1002-1005); "Damaged Claims: {damagedCount} Bks" when >0 (L1006-1011); "Unaccounted Shrinkage Detected" when variance<0 (L1017); "View Damage Proof" button when damagedPhotoUrl (L1020-1024) opening the full-screen viewer (L546-551). Approve/Reject buttons render only when NOT history (L1032-1041).
Same asymmetry as the count panel: "SYS" prints `expectedStock` only, while "FND" is `totalFound` (good+damaged) and variance subtracts expectedStock+expectedDamagedStock.

=== 5. QUARANTINE FLOW ===
There is no quarantine inventory collection. `quarantineInventory` is derived in the effect at L107-155 from live inventory where `damagedStock > 0`, each row tagged with `facility`:
- quarantineFacility 'MASTER' -> filter `safeInventory` (products) (L111)
- 'ALL' -> master plus one listener per branch in `uniqueBranches`, merged (L113-141)
- a branch name -> that branch's `.../branches/{facility}/inventory`, enriched with the master product doc (L143-154)
Sunk Capital header total (L792): `sum(damagedStock * Number(priceDistributor || hpp || 0))`.
Row (L806-836): name (L813), "{damagedStock} Bks Damaged" (L815), "Total HPP Loss" using `priceDistributor || hpp || costPrice` (L807, L817), facility (L819).
All three buttons open the SAME modal via `setResolutionModal({ item, method })`:
- CONVERT TO SAMPLE -> method 'SAMPLING' (L825-827)
- RTV FACTORY      -> method 'RTV' (L828-830)
- PENALTY CHARGE   -> method 'PENALTY' (L831-833)
Modal form (L553-612) submits to ONE handler: `executeResolution` (L334-435, wired at L565).
Common to all three methods:
- qty validated `> 0` and `<= item.damagedStock` (L342), confirmAction (L343)
- `hpp = tierPrice(item, appSettings?.penaltyPriceTier)` (L355; helpers.js:312-315, tier map Retail/Grosir/Ecer/Distributor, default Retail); `totalValue = qty * hpp` (L356)
- targetFacility = `item.facility || quarantineFacility` (L358); itemRef = products/{id} or branches/{targetFacility}/inventory/{id} (L359-361)
- THE ONLY inventory write, identical for all three: `batch.set(itemRef, { damagedStock: increment(-qty) }, { merge: true })` (L363). `stock` is never credited back by any method.
- One log doc to `artifacts/{appId}/users/{masterId}/quarantine_logs` (ref L365, written L429) with: productId, productName, qty, method, facility, totalValueHpp, resolvedBy (email prefix), timestamp (serverTimestamp), details (object, method-specific) — L366-376.
Method-specific extras:
- SAMPLING (L378-390): details = { reason }; PLUS a doc in `.../samplings` (L380-389) with productId, productName, qty, unit:'Bks', reason:`QUARANTINE CONVERSION: {reason}`, sourceId:('VAULT' if MASTER else facility), date:'YYYY-MM-DD', timestamp. logAudit("QUARANTINE_SAMPLING").
- RTV (L392-394): details = { rtvRef } from the "Surat Jalan Retur" input (L587). No other write. logAudit("QUARANTINE_RTV").
- PENALTY (L396-427): details = { agentId, agentName }; PLUS `.../motorists/{agentId}` merged with `cukaiDebts: { PENALTY_{Date.now()}: totalValue }` and `cukaiDebtNotes: { same key: { label:`{name} {qty} damaged`, date } }` (L400-416); PLUS a notification doc (L418-425) with title, message, type:'PENALTY', agentId, isRead:false, timestamp. logAudit("QUARANTINE_PENALTY"). Agent chosen from a `motorists` dropdown excluding 'master_owner' (L596-598).
Liquidation History sub-tab (L840-877) reads `quarantineLogs` (listener L87-92), filtered by regionFilter on `log.facility` (L538-541); each row shows method badge, local time, "{qty} Bks • {productName}", "Facility: {facility} | Executed By: {resolvedBy}", the method-specific detail line (L864-866), and formatRupiah(totalValueHpp) (L871).

=== 6. WHERE THE SYSTEM-EXPECTED QUANTITY LIVES ===
Two fields, two possible documents — the field names are the same in both:
- MASTER VAULT: `artifacts/{appId}/users/{masterId}/products/{productId}` -> `.stock` (healthy, Bks) and `.damagedStock` (quarantined, Bks). Loaded by src/hooks/useDatabaseSync.js:47 and handed down as the `inventory` prop.
- BRANCH WAREHOUSE: `artifacts/{appId}/users/{masterId}/branches/{location}/inventory/{productId}` -> `.stock` and `.damagedStock`. Doc is created/topped-up by src/components/BranchWarehouseManager.jsx:171-177 with `{ productId, name, stock, lastReceivedAt, lastReceivedFrom }` (merge). `damagedStock` is added there only by increments: src/App.jsx:1887 / :1889 (EOD verification credits returned damage) and by StockOpnameView's own approve (L295) and quarantine (L363) writes.
Master product doc fields, per `handleSaveProduct` (src/App.jsx:2876-2905): name, `stock`, `minStock`, `sticksPerPack`, `priceDistributor`, `priceRetail`, `priceGrosir`, `priceEcer` (numeric coercion list at App.jsx:2883), `packsPerSlop`/`slopsPerBal`/`balsPerCarton` (defaults 10/20/4, App.jsx:2890-2894), images, dimensions, useFrontForBack, updatedAt, createdAt. Note `damagedStock` is NOT on the product form — it exists only through increments.
IS IT ALREADY IN SCOPE FOR THE NEW COUNT PANEL? YES — nothing needs to be passed in.
- `activeInventory` (L52) = `isAreaAdmin ? branchInventory : safeInventory`; `branchInventory` comes from the branch listener at L42-50, `safeInventory` from the `inventory` prop.
- `filteredItems` (L529) maps straight off `activeInventory`, so each rendered row's `item` already carries `.stock` and `.damagedStock`.
- The panel already uses them: `getVariance` (L188-199) computes `variance = totalFound - (item.stock + item.damagedStock)`, and the revealed strip prints "SYS EXPECTED: {item.stock || 0}" at L1094.
- It is WITHHELD, not missing: the strip is inside `{isRevealed && ...}` (L1091), where `isRevealed = hasEntry && (goodVal !== '' || damagedVal !== '')` (L1069). Before the counter types anything, the row shows only name (L1075) and "ID: {item.id}" (L1076). The "BLIND COUNT ENFORCED" badge (L627) renders only for non-ADMIN.
- The blind is cosmetic only: the expected value is in client memory the entire time (it is in `activeInventory`), so nothing enforces it.

=== 7. ARE COUNTS PER-FACILITY? HOW IS A FACILITY SELECTED? ===
Yes, counts are per-facility, but the NEW COUNT panel has NO facility selector. The facility is implicit from who is signed in:
- non-ADMIN (isAreaAdmin): the branch listener uses `user.location` (L43-44), so `activeInventory` is that one branch; submit sets auditType 'BRANCH_WAREHOUSE' and branchLocation `user.location || 'HQ'` (L216-217).
- ADMIN (isHighCommand): `activeInventory` falls back to `safeInventory` = master products (L52); submit sets auditType 'MASTER_VAULT' and branchLocation `user.location || 'HQ'`. For the vault owner, `setUser(currentUser)` (src/App.jsx:2232) is the raw Firebase user with no `location`, so it is always 'HQ'. Consequence: an ADMIN cannot count a branch warehouse from this screen at all.
Facility pickers exist only on the other three tabs: `monitorFacility` select (L657-660), `quarantineFacility` select (L771-775), `regionFilter` select (L899-903 and L779-783). All three are populated from `uniqueBranches` (L69-75), derived from `motorists[].location` excluding 'Headquarters' and 'UNASSIGNED' — never from a branches collection listing.

=== 8. PARTIAL / BLIND / CYCLE COUNT TODAY ===
- No cycle count, no count session, no schedule, no ABC/velocity classing, no re-count queue: grep for cycleCount / "cycle count" / blindCount / partialCount / countSession / opnameSession across src returns nothing.
- Blind: convention only, described above (L1069, L1091, L627). Not enforced and not recorded on the audit document.
- Partial: happens implicitly and is invisible. `counts` only holds ids the user typed into, and an entry is deleted when both inputs go blank (L157-165, delete at L162). `handleCommit` submits `activeInventory.filter(i => counts[i.id] !== undefined)` (L202). Uncounted products are simply absent from `items` and untouched by approval — so every count is a partial count by construction, and nothing anywhere records whether it was meant to be full. There is no `expectedItemCount`, no coverage %, no "counted 12 of 80" — the footer only shows "{Object.keys(counts).length} Wares Counted" (L1118).
- Search: `filteredItems` matches `item.name` only (L529), despite the placeholder reading "Scan or Search Product..." (L1058). No ID, SKU, or barcode match.

=== 9. MONITOR TAB (for completeness) ===
`monitorStats` useMemo (L438-505): seeded ONLY from `safeInventory` (master products, L444-448), so a branch-only product id can never appear. Per product: `vault` and `damaged` from the active facility's docs (L450-456); `field` from each matching motorist's `activeCanvas` converted to Bks by packsPerSlop/slopsPerBal/balsPerCarton (L461-472); `sold` from today's `transactions` for agents at that facility (L474-493); `initial = vault + damaged + field + sold` (L497). Card render L671-745. God-mode direct stock override `handleGodModeEdit` (L507-526), DEVELOPER/COMPANY_OWNER only, `updateDoc(ref, { stock: newStock })` at L521.

=== 10. DEAD CODE — DO NOT CONFUSE IT WITH THE LIVE PATH ===
src/App.jsx carries an older, separate whole-vault opname: state `opnameData` (App.jsx:1216), `handleOpnameChange` (App.jsx:2979), `handleOpnameSubmit` (App.jsx:2981-3006) which runs a transaction that OVERWRITES `products/{id}.stock` outright (App.jsx:2996) and logs "STOCK_OPNAME" (App.jsx:2999). Grepping App.jsx and src/components for these identifiers finds only their definitions — nothing renders them or passes them to a child. It is unreachable.

### Mechanism

The New Count row (src/StockOpnameView.jsx:1063-1112) renders name + document id + two blank number inputs and nothing else, because the comparison strip that carries the system quantity is wrapped in `{isRevealed && ...}` at L1091, and `isRevealed` is false until the counter types into Good Stock or Damaged (L1069). That is the deliberate blind-count design, flagged by the "BLIND COUNT ENFORCED" badge at L627 for non-ADMIN users. The data is not missing: `filteredItems` (L529) is derived from `activeInventory` (L52), whose rows are whole Firestore documents carrying `.stock` and `.damagedStock`, and `getVariance` (L188-199) already reads both. Once revealed, the strip prints `SYS EXPECTED: {item.stock || 0}` (L1094) and `TOTAL FOUND: {totalFound}` (L1096) beside a variance chip (L1097), but `totalFound` is good+damaged (L194) and `variance` subtracts `item.stock + item.damagedStock` (L198) — so for any product with damagedStock > 0 the three numbers on screen do not reconcile: the expected figure shown is short by exactly `item.damagedStock`. The same asymmetry is reproduced on the HQ Audits expanded card, which prints `SYS: {item.expectedStock}` against `FND: {item.totalFound}` (L992-994) while the stored `variance` (written at L233) used both expected fields.

### Citations

| file | line | what |
|---|---|---|
| `src/StockOpnameView.jsx` | 14 | StockOpnameView component definition and full prop list (inventory, transactions, db, storage, appId, user, isAdmin, logAudit, triggerCapy, motorists, appSettings) |
| `src/StockOpnameView.jsx` | 27 | isHighCommand = userRole === 'ADMIN'; L30 isAreaAdmin = !isHighCommand |
| `src/StockOpnameView.jsx` | 34 | viewMode state — the four tabs: 'monitor' | 'review' (HQ AUDITS) | 'quarantine' | 'count' (NEW COUNT) |
| `src/StockOpnameView.jsx` | 42 | branch inventory listener: artifacts/{appId}/users/{masterId}/branches/{user.location}/inventory — the area-admin count source |
| `src/StockOpnameView.jsx` | 52 | activeInventory = isAreaAdmin ? branchInventory : safeInventory — the array the count panel iterates |
| `src/StockOpnameView.jsx` | 69 | uniqueBranches derived from motorists[].location, excluding Headquarters/UNASSIGNED — feeds every facility dropdown except the count panel |
| `src/StockOpnameView.jsx` | 80 | pending_audits listener; L83/L84 split into pendingAudits (PENDING_HQ_APPROVAL) and auditHistory (APPROVED|REJECTED) |
| `src/StockOpnameView.jsx` | 107 | quarantine effect — quarantineInventory is derived from damagedStock>0, not a stored collection |
| `src/StockOpnameView.jsx` | 188 | getVariance: variance = totalFound - (item.stock + item.damagedStock) |
| `src/StockOpnameView.jsx` | 202 | handleCommit submits only products the user typed into — every count is implicitly partial |
| `src/StockOpnameView.jsx` | 208 | full pending_audits payload: agentId, agentName, auditType, branchLocation, timestamp, status, items[] |
| `src/StockOpnameView.jsx` | 225 | per-item fields: productId, name, expectedStock, expectedDamagedStock, goodCount, damagedCount, totalFound, variance, damagedPhotoUrl |
| `src/StockOpnameView.jsx` | 239 | addDoc to artifacts/{appId}/users/{masterId}/pending_audits |
| `src/StockOpnameView.jsx` | 242 | AUDIT_PENDING notification written on submit, linkToTab 'stock_opname' |
| `src/StockOpnameView.jsx` | 262 | handleApproveAudit — the approval handler |
| `src/StockOpnameView.jsx` | 273 | approval basePath: branches/{branchLocation}/inventory for BRANCH_WAREHOUSE, else products |
| `src/StockOpnameView.jsx` | 294 | approval writes stock: increment(goodCount - expectedStock), damagedStock: increment(damagedCount - expectedDamagedStock); L299 legacy overwrite fallback |
| `src/StockOpnameView.jsx` | 309 | audit doc marked APPROVED with resolvedAt/resolvedBy; L311 commitInChunks |
| `src/StockOpnameView.jsx` | 320 | handleRejectAudit — sets REJECTED, rejectReason, resolvedAt, resolvedBy; no inventory write |
| `src/StockOpnameView.jsx` | 334 | executeResolution — the single handler behind CONVERT TO SAMPLE, RTV FACTORY and PENALTY CHARGE |
| `src/StockOpnameView.jsx` | 363 | the only inventory write shared by all three quarantine methods: damagedStock: increment(-qty); stock never credited back |
| `src/StockOpnameView.jsx` | 366 | quarantine_logs doc shape: productId, productName, qty, method, facility, totalValueHpp, resolvedBy, timestamp, details |
| `src/StockOpnameView.jsx` | 380 | SAMPLING extra write into .../samplings (productId, productName, qty, unit, reason, sourceId, date, timestamp) |
| `src/StockOpnameView.jsx` | 406 | PENALTY writes motorists/{agentId}.cukaiDebts[PENALTY_{ts}] and cukaiDebtNotes; L418 PENALTY notification |
| `src/StockOpnameView.jsx` | 438 | monitorStats — vault/damaged/field/sold/initial, seeded only from master products |
| `src/StockOpnameView.jsx` | 529 | filteredItems matches item.name only — no ID/barcode search despite the 'Scan or Search Product...' placeholder at L1058 |
| `src/StockOpnameView.jsx` | 627 | 'BLIND COUNT ENFORCED' badge, rendered only for non-ADMIN in count mode |
| `src/StockOpnameView.jsx` | 631 | tab bar (Monitor / HQ Audits / Quarantine / New Count) rendered only if isHighCommand |
| `src/StockOpnameView.jsx` | 992 | HQ Audits expanded row prints SYS: expectedStock vs FND: totalFound — expected excludes expectedDamagedStock |
| `src/StockOpnameView.jsx` | 1063 | the NEW COUNT row: name, ID, Good Stock input (L1081), Damaged input (L1086) |
| `src/StockOpnameView.jsx` | 1091 | comparison strip gated on isRevealed (defined L1069) — why no expected quantity shows before typing |
| `src/StockOpnameView.jsx` | 1094 | 'SYS EXPECTED' prints item.stock only, while the variance chip at L1097 uses stock+damagedStock |
| `src/StockOpnameView.jsx` | 1118 | footer shows only '{n} Wares Counted' — no coverage against the catalogue |
| `src/App.jsx` | 4439 | StockOpnameView mounted for activeTab 'stock_opname' with its prop list |
| `src/App.jsx` | 2232 | vault owner path sets user to the raw Firebase user — no location, so branchLocation falls back to 'HQ' |
| `src/App.jsx` | 2347 | hijackedUser shape: uid forced to bossUid, plus agentId and location used by handleCommit |
| `src/App.jsx` | 2883 | product form numeric fields — stock, minStock, sticksPerPack and the four price tiers; damagedStock is not on the form |
| `src/App.jsx` | 2981 | handleOpnameSubmit — dead second opname path that overwrites products.stock (L2996); never rendered |
| `src/App.jsx` | 1887 | EOD verification increments damagedStock at branch or master — the other producer of quarantine stock |
| `src/hooks/useDatabaseSync.js` | 47 | the `inventory` prop's origin: onSnapshot over artifacts/{appId}/users/{userId}/products |
| `src/components/BranchWarehouseManager.jsx` | 171 | branch inventory doc created with productId, name, stock, lastReceivedAt, lastReceivedFrom |
| `src/utils/helpers.js` | 312 | tierPrice(product, tier) used for the penalty valuation, default Retail |
| `firestore.rules` | 696 | pending_audits rules: read isAreaAdmin, create isSalesman, update/delete false |
| `firestore.rules` | 720 | note that quarantine_logs has no explicit rule and falls through to the owner/distributor-admin catch-all |
| `src/config/permissions.js` | 59 | view_stock_opname granted to TIER_2; the toggle that decides who reaches this screen |
| `src/components/BiohazardTheme.jsx` | 361 | nav entry mapping tab id 'stock_opname' to feature 'view_stock_opname' |

### Smallest fix

Inside `StockOpnameView`'s count-row render in src/StockOpnameView.jsx, the "SYS EXPECTED" label at L1094 should print the same total that `getVariance` (L188-199) subtracts — `item.stock + item.damagedStock`, or split into two labelled figures — so the three numbers in the strip reconcile; and for the non-blind case the strip should be lifted out of the `isRevealed` gate at L1091 rather than having new data threaded in, since `item.stock` and `item.damagedStock` are already on every row via `activeInventory` (L52). The identical relabelling applies to the HQ Audits detail row at L992. No new props, no new listener, no schema change: `expectedStock` and `expectedDamagedStock` are already snapshotted into the audit document at L228-229.

### Could not check

1) Live Firestore data — I read code only, so I cannot confirm which fields actually exist on real product/branch-inventory documents in production, only which fields the code writes and reads. Older documents may carry fields no current code path writes (and legacy audits provably lack expectedStock/expectedDamagedStock, per the fallback at StockOpnameView.jsx:292-299). 2) `quarantine_logs` has no explicit rules block; firestore.rules:713-722 states it falls through to the owner/distributor-admin catch-all, but I did not run the emulator to confirm that the quarantine writes at StockOpnameView.jsx:363/429 succeed for every role that can reach the tab. 3) I did not verify whether firestore.rules matches what is deployed — the repo notes a `.deployed-baseline` file but I did not diff it. 4) Whether `appSettings.usePhotoStorage` being off changes the stored `damagedPhotoUrl` value (savePhotoAndGetReference in src/utils/helpers.js:198 has that branch; I did not read its body). 5) I did not read dist/ bundles or run the app.


---

## ?

**Verdict:** Both failures are real and both are the same root shape — an unguarded async path in front of a UI that has no fallback: the black screen is a rejected `lazy()` chunk import for MerchantSalesView with zero error boundaries in the entire app, which makes React unmount the whole root; the "re-login from Google" is NOT lost auth persistence (persistence is never configured at all, so it is the SDK's IndexedDB default and does survive) but the auth handler stalling on `await`ed Firestore WRITES that can never resolve offline, leaving `user` null in front of a login screen that renders whenever `user` is null.

### Mechanism

(a) BLACK SCREEN. `MerchantSalesView` is code-split: `const MerchantSalesView = lazy(() => import('./MerchantSalesView'))` (App.jsx:41), rendered under one app-wide `<Suspense>` (App.jsx:4008) whose `fallback` is a spinner. `Suspense` absorbs *suspensions*, not *rejections* — a dynamic `import()` that fails to fetch throws, and there is no error boundary anywhere in the codebase (grep across all of src/ for `ErrorBoundary` / `componentDidCatch` / `getDerivedStateFromError` returns zero hits; main.jsx renders `<App/>` bare inside `<StrictMode>`). React 18 responds to an uncaught render error by unmounting the entire root, so `#root` empties and the page becomes the bare dark body — black, unresponsive, only fixable by reload. That matches the report exactly.
  When does the import reject offline? In a production build it should not: dist/sw.js precaches `assets/MerchantSalesView-C3GAC8bV.js` (121 KB, well under workbox's 2 MiB default cap) and every other chunk, including the 1.05 MB `index-CeXTVcW1.js`. But `npm run dev` (package.json:7) registers NO service worker — vite.config.js:21-49 has no `devOptions` block, and VitePWA is dev-disabled by default — and vite.config.js:7-18 documents that his phone reaches the app at `https://192.168.1.141:5173`, i.e. the LAN dev server. In airplane mode that host is unreachable, the chunk fetch fails, `import()` rejects, and the tree is destroyed. The same destruction happens for any other throw inside the terminal's render.
  I ruled out the alternatives you listed: MerchantSalesView has NO Firestore read on its mount path. Every `getDoc`/`getDocs`/`runTransaction` in that file (MerchantSalesView.jsx:887, 907, 1024, 1041, 1166, 1170) sits inside `handleFinalDeal`/sale-commit handlers. Its mount effects are geolocation, an IntersectionObserver, click-outside and keydown listeners only (MerchantSalesView.jsx:313-433). There is no second Suspense and no loading gate inside the terminal.

(b) FORCED GOOGLE RE-LOGIN. Persistence is never set. `setPersistence` and `browserLocalPersistence` are imported (App.jsx:71-72) and `enableIndexedDbPersistence` is imported (App.jsx:97), but grep for `setPersistence(` and `enableIndexedDbPersistence(` across src/ returns ZERO call sites. Auth is created by a plain `getAuth(app)` (src/config/firebase.js:19), so persistence is the firebase-js-sdk browser default chain: indexedDBLocalPersistence → browserLocalPersistence → browserSessionPersistence → inMemoryPersistence. That default is durable and survives a reload, so persistence configuration is NOT the cause.
  The service worker does serve the app shell offline in a production build: dist/sw.js ends with `registerRoute(new NavigationRoute(createHandlerBoundToURL("index.html")))` and precaches index.html with a revision hash. Again, not in dev.
  Nothing signs him out on an offline/auth error. The five `signOut(auth)` call sites are App.jsx:2279 (subscription SUSPENDED), App.jsx:2323 (profile SUSPENDED), App.jsx:2336 (Ghost Killer), App.jsx:2574 (`handleLogout`) and BiohazardTheme.jsx:166 (manual "Terminate Session"). The offline branch of the Traffic Cop's catch sets `OFFLINE_UNVERIFIED` (App.jsx:2410) or takes the isDeveloper bypass — neither signs out.
  What actually shows the Google door is that there is no auth-initialisation gate. `user` starts null and BiohazardTheme renders the sign-in screen on `{!user && (` (BiohazardTheme.jsx:501). `user` is only assigned at the end of the Traffic Cop's serial `await` chain inside `onAuthStateChanged` (App.jsx:2202-2419). Four of those awaits are Firestore WRITES: `await setDoc(sysAdminRef, ...)` (App.jsx:2223), `await deleteDoc(inviteRef)` (App.jsx:2224), and `await deleteDoc(uidRef)` / `await deleteDoc(emailRef)` (App.jsx:2333-2334). Firestore write promises resolve only on SERVER acknowledgement — offline they never settle, by design. `getDocOfflineSafe` (App.jsx:143) was hardened for exactly this and the writes were not. If control reaches any of those lines while offline, the handler hangs forever, `setUser` is never called, and the app sits on the Google login screen indefinitely. That is the re-login he sees. (Preconditions: line 2223 needs the cached `system_admins/{uid}` read to come back non-existent for a masterVIP; lines 2333-2334 need a cached employee record whose motorist profile reads as missing.)

VAULT GRACE — can it survive? The record itself survives: it is localStorage (`kpm-vault-grace`, vaultGrace.js:26) and is only destroyed by `handleLogout` (App.jsx:2574) or `handleAdminLogout`. But the restore effect opens with `const uid = user?.uid; if (!uid) return;` (App.jsx:2455-2457) and only then calls `readGrace(uid)` (App.jsx:2459). `readGrace` also hard-fails on a falsy uid (`if (!uid || record.uid !== uid) return false`, vaultGrace.js:35). So yes — plainly: if the Firebase user object is null after an offline reload, the grace can never restore, no matter how fresh the record is. That is exactly what happens here, because the stalled Traffic Cop never assigns `user`. Two compounding effects: (1) the black-screen unmount also destroys the `pointerdown`/`keydown` listeners that call `touchGrace` (App.jsx:2465-2483), so the 5-minute window stops being refreshed the moment the screen goes black and expires while he is staring at it; (2) even after a successful re-login the record is then stale, so he is asked for the vault password too.

### Citations

| file | line | what |
|---|---|---|
| `src/App.jsx` | 41 | MerchantSalesView is React.lazy — an offline chunk fetch failure here is a rejection, not a suspension |
| `src/App.jsx` | 4008 | the single app-wide <Suspense> that wraps every lazy tab; it has a fallback but no sibling error boundary |
| `src/main.jsx` | 57 | createRoot renders <App/> bare inside StrictMode — no error boundary at the root either (grep for ErrorBoundary/componentDidCatch/getDerivedStateFromError across src/ returns zero hits) |
| `vite.config.js` | 21 | VitePWA config has no devOptions block, so npm run dev registers no service worker and nothing is precached in dev |
| `vite.config.js` | 7 | comment records that his phone tests against the LAN dev server at https://192.168.1.141:5173 — unreachable in airplane mode |
| `dist/sw.js` | 1 | production precache manifest DOES include assets/MerchantSalesView-C3GAC8bV.js and index.html, and ends with NavigationRoute(createHandlerBoundToURL('index.html')) — the shell is served offline in a prod build |
| `src/MerchantSalesView.jsx` | 887 | first Firestore access in the file is inside runTransaction in the sale-commit handler; the mount path has no getDoc/getDocs at all |
| `src/MerchantSalesView.jsx` | 313 | mount effects are geolocation / sessionStorage / listeners only — no network gate that could fail to resolve |
| `src/config/firebase.js` | 19 | auth created with plain getAuth(app) — persistence is the SDK browser default (indexedDBLocalPersistence first), never explicitly configured |
| `src/App.jsx` | 71 | setPersistence and browserLocalPersistence imported but never called anywhere in src/ |
| `src/App.jsx` | 97 | enableIndexedDbPersistence imported but never called anywhere in src/ |
| `src/App.jsx` | 2202 | onAuthStateChanged Traffic Cop — every setUser() lives at the end of a serial await chain |
| `src/App.jsx` | 2223 | await setDoc(sysAdminRef, ...) — a Firestore write promise that never settles offline, and it sits before every setUser call |
| `src/App.jsx` | 2224 | await deleteDoc(inviteRef) — same never-settling write |
| `src/App.jsx` | 2333 | await deleteDoc(uidRef) / deleteDoc(emailRef) in the Ghost Killer — same never-settling writes on the employee path |
| `src/App.jsx` | 143 | getDocOfflineSafe hardened READS against offline; the WRITES above were never given the same treatment |
| `src/App.jsx` | 2410 | the offline catch branch sets OFFLINE_UNVERIFIED and does not sign out — confirming no signOut fires on an offline condition |
| `src/App.jsx` | 2574 | handleLogout is one of only two places that clearGrace(); nothing offline-related clears it |
| `src/components/BiohazardTheme.jsx` | 501 | {!user && (...)} renders the Google sign-in door — there is no auth-initialisation gate, so a stalled handler shows the login screen |
| `src/App.jsx` | 2456 | grace restore effect: const uid = user?.uid; if (!uid) return; — with user null the grace can never be read back |
| `src/utils/vaultGrace.js` | 35 | graceIsValid returns false on a falsy uid, independent of how fresh the record is |
| `src/App.jsx` | 2465 | the touchGrace pointerdown/keydown listeners live in the React tree, so the black-screen unmount stops the 5-minute window from being refreshed |

### Smallest fix

Three small changes, one per failure. (1) Black screen: add a single class error boundary component and wrap the existing `<Suspense>` in App.jsx:4008 with it (or place it in main.jsx around `<App/>`) so a rejected chunk import renders a retry panel instead of unmounting the root — one component, one wrapper line, covers every lazy tab at once, not just the terminal. (2) Forced re-login: drop the `await` on the four Firestore writes inside the `onAuthStateChanged` handler in App.jsx — `setDoc` at 2223, `deleteDoc` at 2224, and `deleteDoc` at 2333-2334 — so the auth handler always reaches a `setUser()` (Firestore already queues those writes locally and flushes them on reconnect; awaiting them buys nothing and costs the whole session offline). (3) Login-screen flash/lock: add one `authReady` boolean set in the `onAuthStateChanged` callback and gate `BiohazardTheme.jsx:501`'s `{!user && ...}` on it, so the Google door only appears once auth has actually reported.

### Could not check

1) Which build he actually tested on. The dev-server theory for the black screen (no service worker → chunk import rejects) versus the production PWA (chunk IS precached) hinges on whether he was on https://192.168.1.141:5173 or the deployed Vercel build, and nothing in the repo records that. If it was the production build, the chunk was precached and the rejection theory needs a second cause — most likely a stale precache after an autoUpdate deploy (dist/sw.js calls skipWaiting + cleanupOutdatedCaches, so a tab still running an old index-*.js requests chunk hashes that were just purged). I could not distinguish these without his console/network log. 2) Whether the dist/ tree in the repo matches what is actually deployed — it is a local build artifact with no deploy timestamp. 3) Whether his device's Firestore cache actually holds `system_admins/{uid}` as a found document. That decides whether App.jsx:2223 is reached at all; if it is cached as found, the hang must come from App.jsx:2333-2334 on an employee account instead. 4) I did not run anything — no build, no dev server, no browser, no airplane-mode reproduction (instructed read-only), so every claim here is static-analysis, not observed behaviour. 5) I did not verify the exact firebase-js-sdk version's getAuth default persistence chain against node_modules; I am asserting the documented browser default.


---

## ?

**REFUTED: false**

The central claim survives the code. useTransactionEngine.js:75-77 sets `currentAgentProfileId = null` for `userRole === 'ADMIN'` outside VEHICLE mode, :79 seeds `finalAgentName` from `user?.displayName`, the only overrides are inside `if (currentAgentProfileId)` at :94, :293, :442 and :522, and the result is persisted at :127, :157, :331, :357, :370, :463, :540, :561. App.jsx:2287-2288 does force a COMPANY_OWNER to `setUserRole('ADMIN'); setAgentProfileId(null);` and App.jsx:304 defaults `adminSalesMode` to 'VAULT', so the default owner sale takes the null branch. The display sites named (MapMissionControl.jsx:1510, ConsignmentFinanceView.jsx:175/178/784) do print the stored string with no motorist lookup, and App.jsx:1727/1735 does bake `displayName - email` into eod_reports. I could not find a guard or sibling path that repairs any of this. What does NOT survive is the proposed fix and two supporting claims — see correction.

**Correction:** PARTIAL — the core mechanism is confirmed, but three specific claims are wrong and the proposed fix is unsafe.

(A) FIX IS UNSAFE — `currentAgentProfileId === null` is not a name gate, it is the vault-vs-van inventory switch. src/hooks/useTransactionEngine.js:251 `if (!currentAgentProfileId && isPhysicallyGiven)` is the ONLY place Master Vault product stock is decremented; :262 is the vault buyback credit. If line 77 stops setting null (their fix), an owner VAULT sale stops deducting warehouse stock entirely and instead reads/updates `motorists/VAULT.activeCanvas` (:292-296, :311). Same switch in handleConsignmentPayment (:484-:512 — returns only go back to `prodRef.stock` in the `else` at :510-ish when the id is falsy) and in App.jsx:3075/3090/3102, where the truthy branch hard-throws `` `${item.name} is not in your vehicle!` `` (App.jsx:3092) — every admin vault sampling would fail. The fix trades a cosmetic name bug for silent stock corruption.

(B) FIX BREAKS THE ONE WORKING RESOLVER THEY WANT TO REUSE. Engine writes `agentId: currentAgentProfileId || 'ADMIN'` (:126, :330, :462, :539). Give the owner a real id and new rows carry 'VAULT', so HistoryReportView.jsx:135 `if (agentId === 'ADMIN')` no longer matches and the else branch does `motorists.find(m => m.id === 'VAULT')` (:143-145) → no doc → stored name kept. App.jsx:1650 (`request.fromAgentId === 'ADMIN' ? (!t.agentId || t.agentId === 'ADMIN')`) also keys on the 'ADMIN' literal.

(C) App.jsx:1489 "SECOND CONTAMINATION PATH" is refuted twice. `activeTrackerId` (App.jsx:1468) reads `user?.agentId` / `user?.role` / `user?.tier`; for the tier-1 owner `user` is the raw Firebase user (`setUser(currentUser)`, App.jsx:2289 and 2232/2374/2389/2412/2419), which carries none of those fields — so activeTrackerId is null and the effect returns at App.jsx:1470. The telemetry write never fires for the owner at all (which also means motorists/master_owner is never auto-created). For an employee `user` is `hijackedUser` (App.jsx:2347-2359) whose `displayName: finalName` (:2350) is already the Fleet name from `liveData.name` (:2317), so the write echoes the Fleet name back rather than overwriting it. Only the `catch` at App.jsx:2340 (live profile sync failed) leaves a Google name to write.

(D) "No migration required, every affected transaction already carries a usable agentId" is FALSE. handleConsignmentReturn writes a RETURN transaction with NO agentId and NO agentName at all — useTransactionEngine.js:588 (and the offline twin at :574). Those rows can never be repaired at read time.

(E) HistoryReportView.jsx:136 gates the ADMIN repair on `if (boss && boss.location)` — `agentName = boss.name` (:139) only runs when the boss motorist doc has a `location`; otherwise it falls to `regionName = 'Headquarters'` and keeps the stored wrong name. Lifting that block verbatim into a shared helper carries the gate.

(F) Two citation labels are inverted/imprecise, not fatal: AgentProfileView.jsx:177 is the doc-EXISTS branch and spreads `...snap.data()` AFTER `name:'Master Owner'`, so a real doc's name wins; :178 is the doc-missing branch. So 'MASTER OWNER' is displayed only when motorists/master_owner is absent or nameless — which, per (C), is the normal state. ConsignmentFinanceView.jsx:60 is three separate equality tests, not the `a === b || c || d` shape quoted.

**Missed entirely:** 1. A THIRD write site with the identical defect, not in their list: StockOpnameView.jsx:215 `agentName: user.displayName || user.email?.split('@')[0] || "Branch Admin"` next to `agentId: user.agentId || 'VAULT'` (:214). Same Google-name bake-in on the opname/audit document; neither part of their fix touches it.

2. The lazy fix they walked past. `appSettings.adminDisplayName` already exists as the intended display name for admin-owned rows (HistoryReportView.jsx:1052), and every display site already special-cases the literal string 'Admin': MapMissionControl.jsx:1510 (`tx.agentName === 'Admin' ? 'Admin' : ...`), HistoryReportView.jsx:955 (hides the SALES line when agentName is 'Admin') and :1052, ConsignmentFinanceView.jsx:175/178 (`|| 'Admin'`), EODReconciliationView.jsx:334 (resolveIdentityName returns 'Admin' on miss). All of that is currently dead code precisely because the seed at useTransactionEngine.js:79 and :431 never yields 'Admin'. Seeding `let finalAgentName = 'Admin';` and letting the existing `if (currentAgentProfileId)` lookups overwrite it (a one-line change in two places, plus App.jsx:1727) fixes the reported symptom without touching id semantics, stock routing, or the ADMIN agentId contract. Their fix changes the id; this one changes only the name, which is what the report is about.

3. Cross-collection id inconsistency they noted but did not flag as a bug: sales write `agentId: 'ADMIN'` for a VAULT owner sale (useTransactionEngine.js:330) while handleSubmitEOD writes `agentId: 'VAULT'` for the same person/mode (App.jsx:1731-1736), and EODReconciliationView filters reports with `r.agentId !== effectiveId` where `effectiveId = isAdmin ? adminSetoranId : agentProfileId` (:75, :125). Any shared read-time resolver has to accept 'ADMIN', 'VAULT' and missing-id as the same identity.

4. Verified-correct claims (no dispute): useTransactionEngine.js:75-77 and :434-436 (ADMIN→null derivation), :79/:431 seeds, gates at :94/:293/:442/:522, writes at :127/:157/:331/:357/:370/:463/:540/:561, returns at :205/:397; App.jsx:299/301/304 defaults, :2230-2231/:2287-2288/:2387-2388 (COMPANY_OWNER→ADMIN+null), :2344-2345 employee contrast, :3055 raw id to engine, :3064-3066 sampling copy, :4351 UI-only 'VAULT'/'ADMIN_VEHICLE', :1656 transfer mass-rename, :1727-1735 EOD; MapMissionControl.jsx:1195/1510; ConsignmentFinanceView.jsx:175/178/784; AgentProfileView.jsx:187/454/1063; HistoryReportView.jsx:132-148/955/1052; MerchantSalesView.jsx:1012/1017/1177/1182/1191; DashboardView.jsx:45; JourneyView.jsx:266/280; EODReconciliationView.jsx:332-335/871.

5. Could not check from code: that motorists/ADMIN_VEHICLE actually holds the name 'MOBIL PAK BOS', and that the observed second name is 'MASTER OWNER' rather than a Fleet-stored value — both are Firestore data, not source.


---

## ?

**REFUTED: true**

Half the diagnosis survives and half does not. Part (a) — lazy chunk rejection with no error boundary anywhere in src/ — is confirmed line by line. Part (b) is refuted: the offline branch the diagnosis cites at App.jsx:2410 to prove "nothing signs him out" is the same branch that calls setUser(currentUser) at 2411, and the masterVIP catch calls setUser at 2387, so the offline path already has two guarded exits that assign `user`. Worse, the first Firestore read in the handler (system_admins/{uid}, App.jsx:2214) is a document that does not exist for ordinary users, so offline it throws into that catch before ever reaching the awaited writes at 2223/2224. The gate at App.jsx:2220 makes 2223 unreachable on any device that has been online once. And the proposed fix for it is actively harmful: removing the await at App.jsx:2333-2334 does not let the handler reach a setUser — it lets it reach signOut(auth) at App.jsx:2336, which offline destroys the credential and produces the exact reported re-login permanently. The fix must be rejected on that ground alone.

**Correction:** PART (a), BLACK SCREEN — citations hold, keep it. Verified: src/App.jsx:41 `const MerchantSalesView = lazy(() => import('./MerchantSalesView'));`; the single `<Suspense fallback={...}>` opens at src/App.jsx:4008 and closes at src/App.jsx:4530, and MerchantSalesView is rendered inside it at src/App.jsx:4336; grep across src/ for ErrorBoundary/componentDidCatch/getDerivedStateFromError/window.onerror/unhandledrejection returns zero hits, and `Suspense` appears only at src/App.jsx:1, 4008, 4530. So "rejected chunk import, no boundary, root unmounts" is supported.

PART (b), FORCED RE-LOGIN — refuted as the explanation for the reported device. The claimed stall is unreachable on the offline paths that actually run:
1. The FIRST Firestore touch in the handler is `getDocOfflineSafe(doc(db,'system_admins',currentUser.uid))` (src/App.jsx:2214-2215), then `system_admins_invites/{email}` (src/App.jsx:2218-2219). Both are documents that do not exist for ordinary users. Offline with no cached copy, `getDoc` rejects, `getDocOfflineSafe` retries `getDocFromCache` and then throws code 'offline-no-cache' (src/App.jsx:143-157). Control therefore lands in the outer catch at src/App.jsx:2379 — which has TWO guarded exits that both assign `user`: the masterVIP God-Mode bypass calls `setUser(currentUser)` at src/App.jsx:2387, and the offline branch calls `setUser(currentUser)` at src/App.jsx:2411. The diagnosis cites 2410 only to prove no signOut fires and never notices that the same branch already sets `user`. Offline, `user` is not left null.
2. The write at src/App.jsx:2223 is gated by `if (inviteSnap.exists() || (isDeveloper && !sysAdminSnap.exists()))` (src/App.jsx:2220), and the masterVIP list is a single address (src/App.jsx:2216). That very setDoc creates `system_admins/{uid}` on the first online login, so on any device that has ever been online the doc exists and the branch is skipped. The 2223/2224 stall cannot happen on a returning device.
3. src/App.jsx:2333-2334 is reachable only for a NON-architect employee whose employee_directory doc is cached AND whose motorist profile is a cached NoDocument tombstone. That is a real but narrow case and it does not cover the architect's own phone, which is the reported device.

THE PROPOSED FIXES:
Fix (2) makes the reported symptom worse, not better. Immediately after src/App.jsx:2333-2334 come `notify(...)`, `signOut(auth)` (src/App.jsx:2336), `setUser(null)` (2337) and `return`. Dropping the `await` does not let the handler "always reach a setUser()" — it lets it reach signOut faster. Offline that destroys the local credential, so the user really is signed out and really must re-authenticate with Google on reconnect, and a reload no longer recovers. Separately, both deleteDoc calls enter the local write queue the instant they are called, awaited or not, so they will flush on reconnect and delete the employee's directory docs on the strength of an offline read; the await is the only thing currently stopping the signOut from following them.
Fix (3) contradicts fix (1)'s own premise. Gating src/components/BiohazardTheme.jsx:501 (`{!user && (`, confirmed) on an `authReady` flag means that during the very stall the diagnosis posits the user sees neither the login door nor the app — a blank screen. If `authReady` is instead set at the top of the callback it does nothing for the stall at all.

FACTUAL CORRECTIONS:
- src/config/firebase.js:23-25 DOES configure Firestore offline persistence: `initializeFirestore(app, { localCache: persistentLocalCache({tabManager: persistentMultipleTabManager()}) })`. "enableIndexedDbPersistence never called" is literally true but reads as "no Firestore persistence" — the modern equivalent is configured, and it is precisely what makes both the cached reads and the never-settling writes possible. (getAuth(app) at src/config/firebase.js:19 is confirmed.)
- vite.config.js:10 says the phone reaches `http://192.168.1.141:5173` — that is the pre-fix state being described; vite.config.js:17 `server: { https: true, host: true }` is the fix. The diagnosis quotes the comment as "https://192.168.1.141:5173". VitePWA at vite.config.js:21 with no devOptions block is confirmed.
- src/main.jsx: createRoot is line 56, StrictMode 57, `<App />` 58, and `<ConfirmHost />` (59) and `<ToastHost />` (60) are siblings, not absent. "renders <App/> bare" is wrong in a way that matters: a root unmount takes the confirm and toast hosts down with it.
- src/MerchantSalesView.jsx: runTransaction is at line 884, not 887; 887 and 907 are `t.get(...)` inside it. Mount effects run at 77, 313, 320, 329, 336, 382, 405, 418, 430, 433, 1304 and 1311 — wider than the claimed 313-433 — but the conclusion holds: none of them touch Firestore, and the only onSnapshot (line 765) is inside a handler.
- vaultGrace.js: `graceIsValid` returns false on falsy uid at src/utils/vaultGrace.js:33, not 35 (35 is the elapsed-time return). The grace-restore effect at src/App.jsx:2455-2460 and the touchGrace listeners at src/App.jsx:2465-2483 are as described. The grace conclusion survives in form but not in force: since offline `user` is set at 2387/2411, the grace can in fact restore offline for the architect.

**Missed entirely:** 1. THE ACTUAL OFFLINE TERMINAL DEFECT, unnamed. The terminal's sale commit runs through `runTransaction` (src/MerchantSalesView.jsx:884). Firestore transactions require a server round trip and cannot succeed offline at all — unlike setDoc/updateDoc/deleteDoc they are not queued. So even if both proposed fixes shipped, the offline terminal would render and then fail at the commit; the item is called "offline-terminal" and the commit path is the part that is definitionally broken offline. Same pattern at src/App.jsx:1783, 2993 and 3070. This changes the fix: an offline-capable terminal needs the commit converted off runTransaction (or an explicit, honest offline refusal at the Deal button), not just an error boundary.

2. THE PRODUCTION BLACK-SCREEN PATH, dismissed too fast. The diagnosis says a production build "should not" reject the import. vite.config.js:22 sets `registerType: 'autoUpdate'`, and dist/sw.js contains skipWaiting, clientsClaim and cleanupOutdatedCaches (verified by grep). A new deploy therefore activates immediately and purges the previous build's chunk URLs while an old tab is still running the old index bundle; that tab's next lazy import has no precache entry and, offline, no network fallback — the same unmount, in an installed PWA, with nothing to do with the dev server. This matters because it removes the "only in dev" caveat that makes the error boundary look optional.

3. THE OFFLINE_UNVERIFIED SCREEN ALREADY EXISTS as the intended offline outcome (src/App.jsx:3988 and its panel with a Retry button, rendered inside `{user && (` in src/components/BiohazardTheme.jsx). If the user reports the Google door instead of this amber "Can't Verify You Yet" panel, that is diagnostic evidence the diagnosis never used: it means `user` was never set at all, which points at the pre-onAuthStateChanged window or a genuine signOut, not at a mid-chain stall (which, per the correction above, would also have to get past 2387/2411 to occur).

4. LOG NOISE RULED OUT SO YOU DO NOT CHASE IT: `logAction` awaits two addDocs (src/App.jsx:2602-2603) that also never settle offline, but grep shows zero `await logAction(` callers in src/, so it is fire-and-forget and is not a live stall.


---

## ?

**REFUTED: true**

The three headline counts all check out against source, but one central mechanism claim and the entire proposed fix are contradicted by code the diagnosis did not read.

VERIFIED AS CLAIMED:
- src/FleetCanvasManager.jsx:1056 (they cited 1057) — `{(selectedAgent.activeCanvas || []).length > 0 && (` is genuinely the only condition wrapping the button at src/FleetCanvasManager.jsx:1059. I grepped every `canEditFleet` and `isReadOnlyMode` occurrence in the file (43, 54, 135, 142, 149, 666, 668-796, 877) — none of them wraps line 1056. The outer wrapper is only `{selectedAgent ? (` at src/FleetCanvasManager.jsx:963. No missed guard.
- src/FleetCanvasManager.jsx:356-357 — `const handleClearCanvas = async () => {` / `if (!selectedAgent) return;` is the whole guard. Confirmed.
- Unsold-only: src/FleetCanvasManager.jsx:387-388 reads the live `activeCanvas`, and that is all it credits. src/App.jsx:1799 and :1810 are exactly as quoted. src/EODReconciliationView.jsx:631 `const countedStock = (agentData.activeStock || []).map(...)` → :657 `remainingStock: countedStock`, and `activeStock: resolvedCanvas` at :264 from :152-154. src/MerchantSalesView.jsx:903 confirmed. Confirmed.
- Red rows: src/FleetCanvasManager.jsx:443, 446, 448, 469, 1074, 1086 all contain exactly what is claimed, and the red predicate really is `item.currentBks > 0`. Confirmed, including the "clearing makes every row red at once" consequence.
- Tier plumbing: src/App.jsx:188, 2209-2210, 2227-2228, 2230, 2284-2287, 4510 all exact. src/App.jsx:4068-4079 prop list exact, no isSystemOwner, no agentProfileId. src/config/permissions.js:2-9, 26, 57, 59, 104 exact. src/components/SettingsView.jsx:213, 1197 exact. src/StockOpnameView.jsx:508 exact (they wrote the path as src/components/StockOpnameView.jsx — that file does not exist; it is src/StockOpnameView.jsx).

WHAT IS REFUTED: see correction.

**Correction:** 1) REFUTED — "Any user who can reach the `fleet` tab and select an agent sees and can fire it", and "a Tier 2 owner, a Tier 3 area admin and a Tier 4 fleet captain all currently get the button [and can fire it]".

They see it. Only Tier 1, Tier 2, and a *delegated* Tier 3/4 can actually fire it. handleClearCanvas ends in `t.update(agentRef, { activeCanvas: [] })` (src/FleetCanvasManager.jsx:414 — they cited 413), and firestore.rules:479-480 is:
  `allow update: if (isAuthorizedAreaAdmin(bossUid) && resource.data.location == getEmployeeProfile().location) || (isSalesman(bossUid) && getEmployeeProfile().agentId == motoristId);`
`isAuthorizedAreaAdmin` (firestore.rules:99-104) requires `canEditRoster == true` AND role AREA_ADMIN/FLEET_CAPTAIN AND same region. The catch-all at firestore.rules:281-283 does not rescue them: `isDistributorAdmin` (firestore.rules:51-54) requires the literal `userRole == 'ADMIN'`, which a Tier 3/4 never has, and `isVaultOwner` (firestore.rules:37) is uid == bossUid. Firestore aborts the whole transaction on the denied write, so a plain Tier 3/4 clicking Reconcile & Clear gets `notify("Failed to clear canvas: ...")` (src/FleetCanvasManager.jsx:419) and nothing moves. That is a UI-says-yes/server-says-no gap, not a data-destruction gap. The real unauthenticated-by-tier destruction surface is Tier 2 COMPANY_OWNER (bossUid null at src/App.jsx:2286 → isVaultOwner catch-all → full write) and canEditRoster-delegated Tier 3/4 in their own region.

2) REFUTED — the proposed fix. `isSystemOwner` is a PLATFORM identity, not a tenant owner: it is true only for a `system_admins` doc holder or the hardcoded `masterVIPs = ['adikaryasukses99@gmail.com']` (src/App.jsx:2209-2210, 2227-2228). Every paying tenant signs in through the COMPANY_OWNER branch where `setIsSystemOwner(false)` runs at src/App.jsx:2238. Gating line 1056 on `isSystemOwner` does not "exclude Tier 2" as a refinement — it deletes Reconcile & Clear for every company that uses the app, leaving it working only inside the architect's own vault. The SettingsView Architect tab is a valid Tier-1-only pattern for a *platform* panel; a fleet-operations button is not that.

3) REFUTED — the fix's implicit premise that no gating decision exists here. firestore.rules:302-321 already documents this exact code, by name, with an emulator-verified decision that goes the other way: "FleetCanvasManager.jsx's Load Canvas / Clear Canvas actions (handleLoadCanvas ~265, handleClearCanvas ~342) are NOT gated behind canEditFleet/canEditRoster" and "NOT gated behind canEditRoster/isAuthorizedAreaAdmin: moving vehicle-canvas stock is a routine fleet-operations action, not roster (hire/fire) management". `allow update: if isAreaAdmin(bossUid) || isFleetCaptain(bossUid);` at firestore.rules:329 was added deliberately so a Fleet Captain could do this. A Tier-1-only client gate silently reverses a written, tested decision.

Minor citation drift (non-material): 1057→1056, 413→414, 396 is the branch *ref* construction — the actual credit writes are src/FleetCanvasManager.jsx:407-411; "1858-1863" for the EOD good-stock credit is actually src/App.jsx:1862-1867 (the damaged credit at 1885-1889 is right); permissions.js TIER_3 view_fleet is line 63 not 64, TIER_4 is 67 not 68; StockOpnameView.jsx is at src/, not src/components/.

**Missed entirely:** 1) THE SECOND PLACE WITH THE SAME DEFECT — `handleLoadCanvas`, src/FleetCanvasManager.jsx:273. Its only guards are `if (!selectedProduct || !loadQty || isNaN(loadQty) || Number(loadQty) <= 0) return notify(...)` (274) and `if (!selectedAgent) return;` (275). Its button at src/FleetCanvasManager.jsx:1047 has NO render condition at all — not even the `activeCanvas.length > 0` one the Clear button hides behind, so it is visible to everyone who reaches the tab even on an empty van. It moves stock the opposite direction (vault/branch → van) through the same `collPath` doc, and firestore.rules:290-292 lets `isSalesman(bossUid)` — i.e. any employee of the company, Tier 5 and 6 included — write `branches/{loc}/inventory`. Gating only Clear leaves the identical ungated twin in place; firestore.rules:302-306 names both in one breath.

2) THE EXISTING IN-FILE AUTHORITY CONCEPT they did not consider — `const canEditFleet = isAdmin || (isAreaAdmin && myProfile?.canEditRoster === true);` at src/FleetCanvasManager.jsx:43, already used to gate the roster controls at :666 and :877, and already the exact shape firestore.rules:479 enforces server-side. If the goal is "not every viewer", `canEditFleet` costs zero new props and matches the rules — versus threading a brand-new `isSystemOwner` prop that no rule mirrors.

3) THE SERVER GATE IS CLIENT-UNREACHABLE ANYWAY FOR THE ONE ACTOR THAT MATTERS. Their step (3), a client-side early return in the handler, is not "a second defence" against a Tier 2 owner: firestore.rules:281-283 gives `isVaultOwner(bossUid)` read+write on `/{document=**}`, so a COMPANY_OWNER can empty any van via a direct SDK call regardless of any JSX condition. Any gate on this action is presentational for Tier 1/2 by construction. Worth stating explicitly before someone treats the fix as a security control.

4) `hijackedUser.uid = trueBossUid || currentUser.uid` (src/App.jsx:2345-2346) is why `const userId = user?.uid` at src/FleetCanvasManager.jsx:17 lands on the right vault even though the component never receives `bossUid` and re-derives its own id (App.jsx computes `const userId = bossUid || user?.uid` at src/App.jsx:323). I checked this specifically because it looked like it might make the whole path unreachable for Tier 3-6; it does not, so it does not rescue the diagnosis — but it is the load-bearing reason the button is reachable at all for those tiers, and nothing in the diagnosis accounts for it.

5) COULD NOT CHECK: whether `activeCanvas: []` written by handleVerifyEOD (src/App.jsx:2000) is what Aldi considers the "sanctioned" clear — I only verified the code path, not intent. Also did not verify src/App.jsx:1990-1995 (`PENALTY_` bounty minting) line-by-line; grep confirms `PENALTY_` occurrences at 1935, 1962, 1978, 1985 in that region, consistent with the claim, but I did not read the block.
